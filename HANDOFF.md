# Front-End Handoff — DSA

**Purpose of this doc:** everything you need to build out the front end without re-deriving what's already been figured out. Claude Code (this repo's `scripts/`, `lib/*-client.ts`, `lib/rating-engine.ts`, `lib/mappers.ts`, and the database itself) continues to be maintained in a separate ongoing session — this doc is the contract between that work and yours.

**If you're a new backend/Claude Code session picking up data-model work instead of front-end work, start at [`DATA-MODEL-HANDOFF.md`](DATA-MODEL-HANDOFF.md) first** — it's a shorter front door into the parts of this doc that actually matter for that (§4 and §6 below), plus the current list of open data-model work. This doc stays the full, canonical history either way.

## 1. What this project is

DSA replaces a Power BI dashboard (`TBL.pbix`, in the parent `OOTP-Analysis` folder, kept for reference) that visualized player ratings, prospect rankings, team rankings, and draft boards for an OOTP Baseball online league ("TheBigLeague"). Instead of Power BI reading manually-exported CSVs, this project pulls live from **StatsPlus** (a third-party web platform the league already uses) into a **Supabase Postgres database**, computes the same proprietary rating system Power BI used (now via TypeScript instead of DAX), and will serve it through a **Next.js** front end on **Vercel**.

Reference docs, in the order you'd actually want to read them if you need the "why":
- [`power-bi-rating-system-analysis.md`](../power-bi-rating-system-analysis.md) (parent dir) — the original Power BI rating engine, reverse-engineered formula by formula, plus the 4 harmonization decisions applied when porting it here.
- [`statsplus-api-inventory.md`](../statsplus-api-inventory.md) (parent dir) — every StatsPlus endpoint, what it returns, auth requirements.
- [`schema-design.md`](schema-design.md) — the reasoning behind the schema (now slightly stale in exact details — see the note at its top).
- [`lib/database.types.ts`](lib/database.types.ts) — **the actual current schema**, auto-generated from the live database. Regenerate anytime with the Supabase MCP `generate_typescript_types` tool, or ask whoever's running the backend session to.
- [`prospect-bio-style-guide.md`](prospect-bio-style-guide.md) — style/tone/data-source rules for the scouting-style blurbs on `/prospects`, worked out against the real top 10 with Rees. Read before writing or regenerating any batch of these.

## 2. Division of ownership

**Cursor was removed from this project 2026-08-28.** Rather than handing `app/**` to a second tool (Cowork), Claude Code now owns the *entire* codebase — backend and front end both. One engineering owner with full repo context, rather than splitting UI work across tools that each have to re-derive the data model from scratch. Cowork's role on this project is deliberately kept outside the codebase entirely — league/GM-facing operations (trades, roster moves, communications), not building or touching `app/**`. If that division changes again later, update this section rather than leaving it stale.

**Claude Code owns the whole repo:**
- `scripts/*.ts` (refresh.ts, compute-ratings.ts, compute-team-ratings.ts, import-draft-pool.ts)
- `lib/statsplus-client.ts`, `lib/mappers.ts`, `lib/rating-engine.ts`, `lib/supabase-client.ts`, `lib/queries.ts`
- All Supabase migrations / schema changes
- `app/**` — pages, layouts, components, styling

**Shared/read-only for both:** the Supabase database itself. Front-end work should treat every table as read-only (query only) — there is no scenario where the front end should be writing to the database directly. All writes happen through the backend's ingestion/compute scripts. This also means front-end work should never hold the `SUPABASE_SERVICE_ROLE_KEY` — see §3's secrets-location note for where that actually lives and why.

## 3. Connecting to Supabase

Project: `DSA`, ref `onclzyjhfkgonemcpcmo`, URL `https://onclzyjhfkgonemcpcmo.supabase.co`.

**✅ RLS is now ON, default-deny, as of 2026-08-19.** Every table has Row Level Security enabled with **zero policies** — meaning only the `service_role` key can read or write anything; the anon/public key gets nothing. This is intentional and expected to stay this way, not a temporary state to "finish" with policies later:

- All existing code (ingestion scripts, front-end server components) uses `service_role` server-side only and is completely unaffected — verified working after enabling.
- **RLS is NOT automatic on a new table.** Caught 2026-08-19: `prospect_bios` (created same day) shipped with RLS *disabled* — a real gap, exposed to the anon key — until the Supabase advisor caught it on an unrelated query days later. Any new `create table` needs its own explicit `alter table ... enable row level security;`, same migration or a follow-up — it does not inherit the project-wide default-deny posture automatically.
- **Why default-deny is the permanent design, not just the current one:** Overall/Potential rounding (hiding exact scout ratings from other GMs) happens in the React display layer, not the database. If anon ever got read access to the raw tables, someone could skip the UI and query Supabase's REST API directly for full-precision values. Routing every read through the Next.js server keeps that rounding meaningful.
- **Practical implication for you:** if you ever add client-side (browser) Supabase calls, they will get zero rows back — not an error, just empty results — until/unless a real anon policy is deliberately added for that specific case. If you hit unexplained empty data and you're using the anon key from the browser, this is almost certainly why. Default to querying through Next.js Server Components (the existing pattern) instead.

You'll need a `.env` file (copy `.env.example`) with `SUPABASE_URL` and `SUPABASE_SERVICE_ROLE_KEY`. The service-role key isn't retrievable via MCP tools for security reasons — it has to come from a human with Supabase dashboard access (Rees), not from asking the backend session to fetch it.

**Where the real key actually lives (updated 2026-08-27):** `platform/.env` itself is git-ignored but is NOT where Rees keeps the real credentials day-to-day — he moved them out of this folder specifically because he's given Cowork access to the `OOTP-Analysis` directory and didn't want Cowork able to see them. The real file lives at `C:\Users\rmvie\secrets\dsa-platform.env`, outside any folder a third-party tool has access to. **Any session that needs to run something requiring these credentials** (`compute-ratings`, `refresh`, etc.) follows the same pattern Rees uses himself:
```
cp ~/secrets/dsa-platform.env .env
# run whatever needs it
rm .env
```
**Do not leave `.env` sitting in `platform/` once the task is done** — that quietly recreates the exact exposure the move was meant to prevent, especially with Cowork's folder access live. Copy it in immediately before the command that needs it, and remove it immediately after — every time, not just "eventually." If two sessions/agents are working in this repo at once, treat "is `.env` currently present in `platform/`" as something to check before considering your own task finished, not something to assume the other session handled.

## 4. Data model — what actually exists

Three layers, all in `lib/database.types.ts`:

### Raw reference data (current-state, upserted each refresh)
`teams`, `players`, `contracts`, `contract_extensions`, `draft_picks`. Field names match StatsPlus's own naming, not relabeled. `players.pos`/`.bats`/`.throws` are StatsPlus's own numeric codes (not letter codes) — decode via `player_ratings_snapshots.pos` instead if you need a letter code like "SP"/"CF". `players.league_id` carries a non-obvious sign convention for level-1 rows — see gotcha 11.

### Raw time-series snapshots (every refresh adds new rows, tagged by `refresh_run_id`)
`player_batting_stats_snapshots`, `player_pitching_stats_snapshots`, `player_fielding_stats_snapshots`, `team_batting_stats_snapshots`, `team_pitching_stats_snapshots`, `player_ratings_snapshots`, `game_results`. **Important:** a player can have multiple stat rows for the same year — one per level they played at (promotions/demotions mid-season). Always filter/group by `level_id` when showing season stats, not just `year`. See §6 for the level-code decode table.

### Computed (rating engine output — never recompute in the front end, just read)
`player_computed`, `team_computed`. Holds `overall`, `potential`, `prospect_potential`, `role`, `sp_rp`, `tbl_pos`, `platoon`, `eta`, and every rank (`rank`, `org_rank`, `prospect_rank`, `prospect_org_rank`, etc.), all tied to a `refresh_run_id` and a `weights_id` (which `rating_weights` row produced them — weights are database rows, not hardcoded, specifically so methodology can be tuned/tested later). **`rating_weights` gained 9 new columns 2026-08-27** (the Contact/Control floor gates + age-developed threshold) — see gotcha 30. **New sibling table `player_projected_splits`, 2026-08-28** — an extrapolated Potential L/R split profile per player per refresh (10 tool grades × L/R), same no-FK/join-by-key pattern as this table — see gotcha 32. **`eta`'s formula changed 2026-08-31**: rounds the years-to-climb distance UP now (was nearest), and floors at `currentYear + 1` for everyone once the league's in its playoffs (`refresh_runs.game_date`'s month ≥ 10) — a same-year ETA stopped making sense once that year's season had already finished. **New columns `comp_player_id`/`comp_similarity`, also 2026-08-31** — the nearest established-MLB-player comp for a prospect (potential grades vs. an established player's current grades, same role bucket, weighted by `rating_weights`). Full methodology: `prospect-comp-methodology.md`. **`scripts/compute-team-ratings.ts`'s farm-system scoring was rewritten the same day** (Rees's spec): `minors_rank`/`batting_prospect_rank`/`pitching_prospect_rank`/`tbl_readiness_rank` are still the same column names, but now driven by a "Blue-Chip + Depth" formula (top 3 prospects per H/P split count in full, everyone lower decays by rank) combined with an explicit balance penalty so a lopsided batting-only or pitching-only system can't out-rank a well-rounded one — replaces the old flat top-20-average port of the Power BI "RLB" system. New `system_rank_weights` table (mirrors `rating_weights`' pattern) holds the tunable coefficients; new `team_computed.balance_index` column is a display-only 0-1 "how lopsided" stat. Full methodology: `system-rank-methodology.md`. See gotcha 34 for a real bug this same-day migration introduced elsewhere and how it was fixed. **New columns `blue_chip_score`/`depth_score`, also 2026-08-31** — the combined batting+pitching Blue-Chip and Depth components broken out separately (they already summed into `minor_league_batting_rating`/`minor_league_pitching_rating`; these are purely a display decomposition for the rebuilt System Rankings cards' 3-way grade breakdown, not a new number). **New table `org_system_bios`** — hand/AI-written system-analysis paragraph per org, same infrastructure pattern as `prospect_bios` (ships empty; the writing pass is a separate follow-up step).

**System Rankings page rebuilt into cards, 2026-08-31** (`/TBL/prospects/farms`, Rees's spec) — same card language as Top Prospects (`.prospect-card--clickable`/`.prospect-bio-expanded` reused verbatim for the org-analysis toggle), but allowed to run taller: team header with all 4 ranks color-coded via the same `percentileStyle()` gradient used on `/players`/`/draft`/`/org-minors`, a two-column top-5-hitters/top-5-pitchers breakdown, and a Blue-Chip/Depth/Balance grade-word breakdown (NOT the underlying numbers, per Rees's spec). New query module `lib/system-rankings-query.ts` (`getSystemRankingsDetailed()`) and component `app/_components/SystemRankingsCards.tsx` power this specifically — the OLD `getTeamRankings()`/`TeamRankingsTable.tsx` are UNCHANGED and still power `/prospects`' compact side-by-side rankings column, which doesn't have room for these taller cards. **Balance Index grading, settled after a same-day back-and-forth**: briefly switched to fixed ABSOLUTE thresholds (0.90/0.75/0.55/0.35) after noticing the whole league clusters tightly at 0.85-0.98 balance, which made percentile grading put an objectively fine 0.91 at "Below Average" just because a few orgs sat higher. That in turn caused the opposite real problem Rees caught immediately: Steam graded "Plus" balance (0.85 clears the absolute bar) despite a #3 batting rank next to a #27 pitching rank -- a genuinely lopsided system competitively. **Reverted to percentile-among-orgs**, same as Blue-Chip/Depth, on Rees's explicit call: balance should reflect standing relative to the rest of the league, not an absolute ratio read in isolation. Full back-and-forth: system-rank-methodology.md §5a.

**`role` for position players was redesigned 2026-08-20** (pitchers unchanged — still `sp_rp`-based). Old formula used only composite *current*-ability ratings (`cRating`/`infRating`/`ofRating`) with a coarse INF/UTIL/C/OF bucket set. New formula, Rees's spec, in `lib/rating-engine.ts`'s `ROLE_BUCKET_THRESHOLDS`: seven buckets — **C → SS → CF → INF → COF → 1B → DH** — evaluated in that priority order, first match wins, mixing *potential* position-fit grades (`pot_c`/`pot_ss`/`pot_cf`/`pot_lf`/`pot_rf`/`pot_1b`) with *current* range grades (`ifr`/`ofr` — no potential-range field exists in this data). Exact cutoffs: C `pot_c≥50`; SS `pot_ss≥55` + `ifr≥65`; CF `pot_cf≥55` + `ofr≥65`; INF (2B/3B) `ifr≥50` alone, no position-potential gate; COF `max(pot_lf,pot_rf)≥50` + `ofr≥50`; 1B `pot_1b≥55`; DH is the pure fallback (didn't clear any bar). Verified against 8 known players spanning every non-DH bucket, including two priority-order edge cases (a player with qualifying `pot_1b` correctly landing INF instead, since INF is checked first). Distinct from `tbl_pos` (still unchanged — lists *every* position a player's potential clears a bar for, not a single bucket).

### Draft class tracking
`draft_class_imports` / `draft_class_pool_members` — a manually-imported OOTP export (not from StatsPlus) giving the *exact* roster of a given draft class. Necessary because StatsPlus's `draft_eligible` flag on `players` is **not** scoped to one class — it's a broad multi-year amateur flag. Always join through `draft_class_pool_members` for "this year's draft pool," never filter on `draft_eligible` alone.

### Transaction/trade analysis — started 2026-08-31 (Rees's ask)
Goal: track trade/transaction history to identify market prices, cross-reference team needs, and eventually suggest transactions — Claude Code builds the analysis, Cowork acts on it (drafting/communicating), same division of labor as everywhere else on this project. Investigated live before building anything (don't re-derive this — see `statsplus-api-inventory.md` if it needs updating):

- **Free agency needs zero new work.** `players.free_agent` is already ingested in bulk. The raw flag alone is noisy (37,050 rows, mostly amateurs who've never been rostered) — filter to `free_agent = true AND last_team_id IS NOT NULL AND last_team_id != 0 AND retired = false` for the real, actionable list (confirmed: 2,262 rows).
- **Trade block — Phase 1, shipped.** `trade_block_snapshots` table + `scripts/scrape-trade-block.ts`, scraping `https://.../thebigleague/tradeblock/` (public, no auth — the player_id→asking-price-note map is baked directly into that page's HTML as a `<script id="tradeblock-pids-data">` JSON blob, one page fetch for the whole league, no per-player scraping). Not yet wired into the automated refresh pipeline — trade block content is GM-driven, not sim-driven, so its own run cadence is still an open decision.
- **Trade history — Phase 2, shipped, then REWRITTEN same day after Rees caught a real accuracy gap.** First version scraped `/player/{id}?page=trade` for the 276 `players.was_traded=true` players and landed at 195 trades. Rees flagged this as suspiciously low against StatsPlus's own `/trade/#recent` tab, which he could see showing **1,101 trades since 2001**. Investigating that tab found the real problem: it's backed by a public, no-auth AJAX endpoint (`/ttajaxtable/?info=recent&view=recent`) that returns the league's entire trade ledger in one ~3-second request — and cross-checking it directly showed only **274 of the 1,749 players actually involved in a real trade have `was_traded=true` (16%)**. The flag itself is badly incomplete; scoping to it was a bad premise, not a scraping bug. **Rebuilt entirely around the ledger endpoint instead** — no player-list dependency at all, one request pulls all of league history. `lib/statsplus-client.ts`'s `playerTradeHistoryHtml()` was removed and replaced with `tradeLedgerHtml()` (probes for the `total` count, then re-requests with `rows=total+50` to get everything in one page — confirmed byte-identical across two independent fetches, so row order is stable). `scripts/scrape-trade-history.ts` is a full rewrite around this endpoint, not a patch.
  - **Verified final state: all 1,101 trades (1,084 complete + 17 pending) and 4,101 items — 2,138 players, 1,700 draft picks, 263 cash entries.** Two entirely new asset types the old per-player-page approach never saw at all: **draft picks** (`trade_event_items.pick_year`/`pick_round`/`pick_team_id`/`pick_team_name` — the pick's *original* team, which can differ from either side of a given trade if it already changed hands once before) and **retained-salary percentages** on player items (`retained_salary_pct`, e.g. a team keeping 50% of a traded player's salary) — directly useful for the still-unsolved "team payroll isn't a simple salary sum" gap below. New `trade_events.status` column (`'complete'`/`'pending'`) carries StatsPlus's own trade-review status through. Confirmed idempotent by test — re-running against already-populated tables reproduces the exact same 1,101/4,101 counts.
  - **Team abbreviations get reused across league history — do not build an abbreviation→team lookup without checking for collisions.** Building an index to resolve a traded pick's original team from its 3-letter code (e.g. "DEN") found two real collisions across 30 years: `BOS` and `NY` each map to two different team ids, almost certainly franchise relocations/renames reusing a short code. The script only resolves a pick's `pick_team_id` when its abbreviation maps to exactly one id across the whole ledger; ambiguous or unseen abbreviations (121 of 1,700 picks, ~7%) keep `pick_team_id` null with `pick_team_name` holding the raw code as a fallback — same "don't guess wrong" principle as the team-resolution gotcha below.
  - **A trade's own team cells resolve far more completely than the per-player page's did.** The old approach hit an unlinked "Durham" team as a hard-to-resolve edge case. The ledger endpoint links that exact same historical trade correctly (to team id 17 — a franchise since renamed to Cincinnati in `teams`, `team_a_name` still preserving "Durham (DUR)" as it read at the time) — **zero trades in the full 1,101 ended up with an unresolvable team side.** `extractTeamRef()`'s null-id/name-fallback path is kept as a defensive backstop, not because it's regularly needed here.
  - **A trade's own free-text "Note:" field (12 found, e.g. "Note: Retain 25%", "Note: Plus Ryan Thompson and the 2008 1st Round Pick from Huron") is GM commentary, not a distinct asset** — every note spot-checked described something *already present* in that trade's own structured player/pick items, never something missing from them. The script logs and skips these rather than treating them as unparseable content.
  - **Trade-key design, carried forward from the first version's own hard-won lesson (see git history if the reasoning is needed) with one adjustment**: since this endpoint returns the complete ledger fresh every time (not fragments merged across many player pages), there's no risk of a parsing fix changing what's captured for an already-keyed trade the way there was before. Key is still `(date, sorted team pair)`, but 31 real combos in this ledger have more than one distinct trade on file (e.g. two separate Carolina/St. Louis deals same day) — disambiguated by position within that group, in the ledger's own fetch order, which is confirmed stable across independent fetches.
  - Not yet wired into the automated refresh pipeline — same open cadence question as the trade block scraper above (run cadence for both still needs a decision: on every StatsPlus refresh, on a slower schedule, or manual-only).
  - `view: "void"` on the same endpoint is a separate 85-row list of *cancelled* trades that never went through — not pulled, not part of the 1,101, flagged here in case it's ever wanted.
- **Two bonus discoveries, not yet used for anything:** `/thebigleague/finance/current/` is a real, game-computed team payroll/budget report — may solve the "team financials" gap below without needing to reconstruct retained-salary math ourselves. `/thebigleague/rosterconstruction/` breaks every team's roster down by acquisition method (draft/trade/FA/waiver/international/etc.) plus a player WAR-vs-salary efficiency table — directly relevant to "team needs" and value analysis.
- Team-needs analysis itself (the next real step) has real raw material already: org-minors depth data, role/level benchmarks, prospect rankings. Likely more an aggregation task over existing data than new ingestion.
- **Trade-value methodology, proposed 2026-08-31 (not yet built).** Goal: score both sides of a trade to answer "who won this" — four components: (1) talent value, reusing existing Overall/Potential/Prospect Potential/role/ETA rather than a new model; (2) contract surplus value — market rate for a player's talent tier (regressed from real league salaries) vs. what they're actually paid, with `retained_salary_pct` (see above) directly discounting the acquiring side's effective cost; (3) a control-years multiplier, discounted for years further out, so a pure rental scores far below a multi-year-control player at the same talent+salary level; (4) draft pick value, built empirically from our own league's real draft-outcome history (round → eventual career value) rather than a borrowed external chart, further discounted for picks further in the future. Combine via a weighted composite (same `rating_weights`/`system_rank_weights`-style tunable-DB-table pattern as everywhere else, not hardcoded), not a full WAR-surplus dollar model — the latter would need an aging/dev curve we don't have enough history to fit reliably yet.
  - **Two real data-availability walls found while scoping this, both now addressed (except history that hasn't happened yet):**
    - Ratings/computed snapshots (`refresh_runs`) only go back to **2031-06-23** (our first successful refresh — coincidentally right around that season's trade deadline). Of the full 1,101-trade ledger, only **2 completed trades** fall on/after that date (both 2031-07-07), plus 17 sitting in the pending-review queue as of this writing. Any trade before 2031-06-23 has no "ratings as of the trade" to score against and never will — those are permanently out of scope, exactly as Rees expected going in. The testable sample grows only as the league keeps simming and trading from here forward.
    - Contract data (`contracts`/`contract_extensions`) was current-state-only — overwritten every refresh, no history — making "contract terms as of a past trade" unreconstructable beyond a short recent window. **Fixed 2026-08-31**: new `contract_snapshots`/`contract_extension_snapshots` tables, same no-FK/sibling/`(refresh_run_id, player_id)`-keyed pattern as every other snapshot table, populated every refresh from the exact same `sp.contracts()`/`sp.contractExtensions()` rows already being pulled for the current-state tables (`refresh.ts`, no second fetch). First real capture: refresh run 25, 13,899 rows each. The existing current-state `contracts`/`contract_extensions` tables are untouched and still serve anything that just wants "the deal right now." **This only accumulates going forward — there is no way to backfill contract history for trades that already happened**, which is exactly why Rees asked to get this capturing now, before the offseason's trading activity.
  - **Contract-classification refinement, 2026-08-31, before building the market-rate curve**: Rees pushed back on a naive salary-vs-Overall regression, correctly — OOTP (like real MLB) pays players on a fixed, escalating scale by service time, totally independent of talent. Confirmed directly in this league's own data: pre-arb averages **$122K**, arbitration-eligible **$2.64M**, free-agent-eligible **$9.30M** — regressing across all of that would mostly measure service time, not skill. Checked every possible better signal before settling on the fix: `players.has_received_arbitration` is the field StatsPlus clearly intends for this and it's simply unpopulated (`false` for all 13,899 non-retired players); no StatsPlus endpoint or report page (finance, roster, free agents) exposes an explicit contract-type flag. Landed on a multi-signal empirical classification instead of a single service-year cutoff: (1) league-minimum salary detected fresh each run as the mode of low-service salaries (confirmed a real, sharp constant — $500,000 as of 2026-08-31 — not a fuzzy proxy); (2) 6+ years of MLB service required (confirmed 85% of 3–5-year arbitration-eligible contracts are exactly 1 year, matching how real arbitration works: awarded one season at a time); (3) excludes anyone also present in `contract_extension_snapshots` (catches a below-market extension signed before free agency — small in practice, 8 players, but a real and free cross-check). Only contracts clearing all three feed the curve; every player (including a rock-bottom-paid rookie) still gets *scored* against it — that gap between the curve's prediction and actual pay is exactly what "surplus value from cost control" means.
  - **Market-rate curve — first piece built and verified, 2026-08-31.** `scripts/compute-market-rates.ts` fits `ln(AAV) = intercept + slope × Overall` via OLS on the clean sample above (log-linear, not linear — pay scales disproportionately with elite talent), then computes a per-role multiplier (each role's real average pay ÷ what the curve alone predicts for their average Overall) — this is where "a top reliever isn't worth what a top starter is worth" gets captured from what this league's own GMs actually pay, not a hand-picked weight. Two new tables, `market_rate_curves` and `market_rate_role_multipliers`, versioned by `refresh_run_id` like `player_computed`/`team_computed` (computed output, not a manually-tuned weight table like `rating_weights`).
  - **First result exposed a real bug, caught by Rees, fixed same day.** The very first version pooled hitters and pitchers into ONE curve — RP's genuinely lower pay dragged that pooled baseline down, making every hitting position look inflated relative to it, and DH came out priced **highest of any role (×2.23)**, above every position that also plays defense. Rees called this out as inherently wrong: DH provides zero defensive value and can never legitimately outrank a position that does. **Fixed with three changes**: (1) split into two separate curves, hitters-only and pitchers-only, fit independently — removes the cross-contamination; (2) every role's raw multiplier is shrunk toward 1.0 by sample size (`shrunk = 1 + (raw−1) × n/(n+25)` — a small sample like DH's n=11 keeps ~30% of its raw signal, a well-supported one like RP's n=75 keeps ~75%) — a standard fix for exactly this "noisy small-sample ratio" problem; (3) DH is explicitly capped at the lowest other hitting-role multiplier post-shrinkage, since no amount of data should ever let DH out-rank a fielder — a domain constraint no regression can infer on its own. **Corrected result** (same 264-player sample): DH now correctly lands at the bottom, tied with C (×0.96, capped down from a raw 1.88) — SP (×1.29) and RP (×0.96) show a sensible, real starter-over-reliever premium now that they're compared only to each other, not blended with hitters. COF remains the one number still worth watching (×1.26, down from ×1.84 pre-shrinkage) — smallest hitting sample after DH (n=23), plausible but not fully trusted yet.
  - `contract_snapshots`/`player_computed` don't always share the same latest `refresh_run_id` — contracts refresh every run (no auth needed), ratings only refresh on a session-authenticated run (rarer, sometimes deliberately skipped). The script finds each table's own latest run independently rather than requiring a match — confirmed real as of this build (`contract_snapshots` on run 25, `player_computed` on run 24) and treated as an accepted, reasonable join (a player's Overall/role rarely shifts meaningfully over a small gap between refreshes) rather than a bug to chase.
  - **Accumulation build — shipped 2026-08-31.** Shrinkage only ever compensated for a small sample; this is what actually grows it. New append-only `market_rate_training_contracts` table, one row per *distinct* clean contract ever observed, keyed on `(player_id, season_year, years, salary0)` so an unchanged contract showing up in yet another snapshot is a no-op, not a duplicate — only a genuinely new or renegotiated contract adds a row (naively pooling every snapshot's clean rows would have massively over-counted a still-active 3-year deal appearing in 15+ consecutive snapshots). `lib/contract-classification.ts` extracted the "what counts as clean" logic (AAV calc, league-minimum detection, the service-years + extension-exclusion rules) out of `compute-market-rates.ts` into a shared module, so it can't drift between the two scripts that now need it. `scripts/scan-market-contracts.ts` (new) diffs each refresh's clean contracts against what's already recorded and appends only real changes — wired into `refresh.ts`, runs every refresh, own try/catch so a failure there doesn't fail the whole refresh. `compute-market-rates.ts` now fits against the full accumulated table instead of deriving a cross-section itself. Verified: seeded from the existing single snapshot (264 contracts, exactly matching the prior cross-section), re-ran the fit against the accumulated table — identical results confirmed the refactor is correct.
  - **`/admin/market-rates` — visualization + tuning page, shipped 2026-08-31.** Owner-only (nothing under `/admin` is in `middleware.ts`'s `GUEST_ALLOWED_PATHS`, so no new auth work needed). New `lib/market-rate-query.ts` data layer (curves, role multipliers, and training contracts joined to player names — no FK between `market_rate_training_contracts` and `players`, same join-by-key pattern as gotcha 1). `MarketRateExplorer.tsx` (client component, new `recharts` dependency) renders: an Overall-vs-AAV scatterplot color-coded by role with both the hitter and pitcher regression curves overlaid, a hover tooltip showing player/role/AAV/contract length, role-filter chips and a hitter/pitcher/all toggle that both filter the scatter and rebuild the curve overlay live; fit-stat cards per curve (equation, R², residual SD, n) — `market_rate_curves` gained `r_squared`/`residual_std_dev` columns for this, computed the standard way (in log-AAV space, the space the fit actually happens in) alongside the existing intercept/slope; a per-role summary table (n, avg Overall, actual vs. curve AAV, raw/shrunk/final multiplier, a 🔒 marker on DH's capped row) that's clickable to drill into that role's individual underlying contracts, sorted by AAV. Verified end to end in a real dev-server run against live data: all 264 dots and both regression lines render, the DH row's drill-down correctly surfaces its real 11 contracts (immediately visible why DH's raw multiplier ran hot: a single $36.64M/5yr Antonio Reyna deal in an 11-player sample).
  - **Hover/click interactivity was actually broken on first ship, fixed same day.** The built-in recharts `<Tooltip>` never fired on hover in real use (Rees confirmed) — recharts v3 rewired its internal event dispatch, and multiple `<Scatter>` series sharing one implicit `<Tooltip>` didn't activate the way v2-era examples assume. Root-caused by reading recharts' own source (`node_modules/recharts/es6/cartesian/Scatter.js`): `Scatter` directly supports `onMouseEnter`/`onMouseLeave`/`onClick` props, dispatched per-symbol. Replaced `<Tooltip>` entirely with these — own `hoveredPoint`/`pinnedPoint` state drives an always-rendered detail panel above the chart (no layout jump): hovering shows a live preview, clicking pins a point so it stays visible after the mouse moves away (a "clear ✕" appears on a pinned, non-hovered point), directly satisfying "hover or click" rather than hover-only. Verified for real this time — a dispatched `mouseover` on a rendered symbol updated the panel with genuine contract data, `mouseout` correctly cleared it back to the placeholder, and `click` correctly pinned it. (The first version's "hover tooltip is wired correctly" claim was based on confirming the Tooltip *mounted*, not that it displayed real content on real interaction — didn't catch that it was actually broken. Lesson: confirming a DOM node exists is not the same as confirming an interaction works.)
  - **Nav bugs from the same page shipping, both fixed 2026-08-31**: (1) `SiteNav.tsx`'s links wrapped to a second line once "Reports" became the 7th item — root cause was a cramped shared row (only ~450px left for links after the brand and the theme-toggle/game-date/guest-preview cluster claimed their share of a 1200px-max header at a normal 1280px window, but the links needed ~700px). Fixed by giving `.site-nav-links` its own full-width row below brand+actions (`flex-wrap` on `.site-nav`, `flex-basis: 100%` + `order: 3` on the links row) instead of squeezing everything into one row — comfortably fits with room to spare now, confirmed via real measurement (0 overflow at 1280px). (2) The "My Team"/"Reports" dropdown menus closed before they could be clicked — `.site-nav-dropdown-menu` had `margin-top: 0.25rem`, a gap that sits OUTSIDE `.site-nav-dropdown`'s hoverable box (`position: relative` doesn't expand a parent to cover an absolutely-positioned child), so moving the mouse from trigger to menu crossed a dead zone and lost `:hover` before arriving. Fixed by setting the menu flush against the trigger (`margin-top: 0`, still `top: 100%`) and moving the lost visual spacing into `padding-top` instead, which IS inside the hoverable box. (`:hover` state can't be verified via dispatched synthetic events in this environment — this is a well-understood CSS gap-hover bug and fix, confirmed correct via computed-style inspection, not a live mouse test; worth Rees's own confirmation.)
  - **The nav fix above immediately caused a second regression, fixed the same day**: the "own full-width row" fix left `overflow-x: auto` on `.site-nav-links` as a safety net for narrow windows — but per the CSS overflow spec, setting only `overflow-x` forces `overflow-y` to also compute as `auto`, which clipped the dropdown menus (absolutely positioned, extending below that row's own box) completely out of visibility. Went from "closes too fast" to "doesn't work at all." Removed the safety net entirely — the full-width-row fix already solves the space problem structurally (confirmed 0 overflow at 1280px), so it wasn't earning its keep. Verified via computed styles (`overflow-x`/`overflow-y` both back to `visible`) and via `elementFromPoint` on a force-shown menu confirming the link is the actual topmost, clickable element at its own coordinates — not just present in the DOM.
  - **Known methodology gap, flagged by Rees 2026-08-31, deliberately not fixed yet: `market_rate_training_contracts.overall` is the player's CURRENT Overall, not their Overall at the time that contract was signed.** For a contract signed years ago, if the player has since progressed or regressed, the training point compares an old AAV against the wrong talent level — real noise in the curve. Checked how big this actually is right now: of the 264 training contracts, only 136 (52%) were signed in 2031, the year ratings-snapshot coverage even starts (2031-06-23) — the other 128 (48%, signed 2027-2030) are almost certainly affected, and even some of the 136 "2031" ones could predate the actual 2031-06-23 coverage start (`season_year` is year-only, not a full date, so this can't be checked more precisely than that from what's stored). **Deliberately not filtered out now** — cutting the sample roughly in half would trade one small-sample problem for another, same tension as everything else in this build. **The real fix, once enough post-2031-06-23 signings have accumulated**: filter `market_rate_training_contracts` to only contracts signed on/after the league's first ratings-snapshot date, so `overall` is at least a contemporaneous read on the player rather than a current one applied retroactively to an old deal. Revisit this once the accumulation system (above) has meaningfully grown the post-coverage-start population — check the season_year distribution before deciding it's ready.
  - Remaining pieces before a first end-to-end trade score: talent-value blend, control-years multiplier, draft-pick value curve, then the final weighted composite. Next step when picked back up: build those, smoke-test against the 2 real completed trades + the pending ones (not a real backtest — the sample is just too thin yet), then revisit/retune once more real trades accumulate under full snapshot coverage.

### Rating-engine validity check — `/admin/rating-validation`, shipped 2026-08-31 (Rees's ask)
Separate from the trade-value work above — frames a potential future redesign of the grading/weighting system itself by testing whether Overall (and each individual weighted grade that feeds into it) actually predicts real production. Same interactive pattern as `/admin/market-rates` (scatter + regression + hover/click detail panel via explicit `Scatter` handlers, reused deliberately — see `lib/regression.ts`, extracted from `compute-market-rates.ts` so both pages share one OLS implementation).

- **Scope**: real 2031 MLB WAR only (`level_id=1`, `split_id=1` — confirmed the "overall" split; 2/3/21 are situational splits with WAR always 0). Compared against Overall and every raw grade that actually feeds the engine's Overall formula (`lib/rating-engine.ts`): hitters — Contact, Gap, Power, Eye, Avoid-Ks, Speed; pitchers — Stuff, Movement, Control, Stamina, PBABIP. **Potential is deliberately excluded** — it's a forward-looking ceiling projection, not a claim about this year's production, so testing it against this year's WAR wouldn't be a fair comparison the way Overall-vs-WAR is.
- **A real bug caught during verification, not a live-site issue (caught locally before shipping)**: the first version summed `war` across every row returned for a player+2031+level+split — and got an impossible range (individual-season WAR from -55 to +165). Root cause: `player_batting_stats_snapshots`/`player_pitching_stats_snapshots` are **cumulative-as-of-refresh-date time series**, the same pattern as ratings/contracts (documented elsewhere in this doc, apparently not internalized deeply enough the first time this table was queried for a sum) — confirmed directly: one player had 22 rows for the 2031 season, each refresh_run_id's own running total (249 PA at run 3, climbing to a final 692 PA by run 18+), not 22 independent stints. Summing across every refresh_run_id inflated everyone's WAR by roughly how many times they'd been snapshotted. **Fixed**: query only the single latest stats `refresh_run_id`; multiple rows *within* that one run (a real same-season trade producing 2 team-stint rows in one final snapshot) still get summed correctly, just never across different runs. Re-verified after the fix: WAR range came back to a realistic -4 to +12 (hitters) / -3 to +9 (pitchers). **General lesson, worth remembering before writing any new query against a `_snapshots` table**: check whether "sum across all rows for this key" is even the right operation before writing it — for an append-only accumulation table (like `market_rate_training_contracts`) summing everything is correct; for a cumulative-per-refresh snapshot table (ratings, contracts, and now confirmed also true of the stats tables), it is almost never correct, and the failure mode is a plausible-looking number that's actually wildly wrong, not an error.
- **A second real bug, also caught locally before shipping**: `lib/rating-validation-query.ts` exports plain runtime constants (`HITTER_VARIABLES`/`PITCHER_VARIABLES`), which `RatingValidationExplorer.tsx` (a `"use client"` component) imports as real values, not just types. That pulls the WHOLE module into the browser bundle — including a module-top-level `makeSupabaseClient()` call, which then executes in the browser and throws immediately (`SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set`), confirmed via a real dev-server hydration crash. `market-rate-query.ts` has the identical top-level-client pattern but was never triggered, purely because `MarketRateExplorer.tsx` happens to only `import type` from it (type-only imports are erased at compile time, so the module's runtime code, including that top-level call, never actually ships to the browser). **Fixed in both files**: `makeSupabaseClient()` is now called inside each exported function instead of at module top level, so merely importing a module for its types or constants can never execute server-only code. **General lesson**: a data-layer module meant to be safely importable from a client component should never instantiate anything server-only (a Supabase client, an API key read) at module top level — do it inside each function instead, even if nothing currently imports a runtime value from that particular file. The bug is latent until some future edit adds exactly that kind of import, at which point it silently ships a server credential-reading call to the browser.
- **First real results** (516 hitters / 511 pitchers, 2031 MLB): Overall's R² is meaningfully higher than any single raw grade for both player types (hitters: Overall 0.431 vs. best single grade Gap at 0.136; pitchers: Overall 0.453 vs. best single grade Movement at 0.240) — a good sign for the composite formula, as expected (a well-built composite should outperform any one ingredient). **One finding worth flagging as a real, actionable signal, not just a data point**: for pitchers, **Stuff has the *weakest* individual R² of all five grades (0.029)**, while **Movement is by far the strongest (0.240, more than double the next-best, Control at 0.154)** — worth a real look at whether `rating_weights.stuff` is weighted appropriately relative to `rating_weights.movement` once more seasons of data confirm this isn't a one-year fluke.
- **Refined to a rate stat with role-aware minimums, same day, Rees's follow-up.** Raw season WAR was rejected as the target: it rewards playing time as much as talent (an average player at 600 PA out-totals a great one at 200), and it can't compare starters and relievers at all (an ace SP racks up 3-4x an elite RP's innings). Now: hitters are measured on **WAR per 100 PA, minimum 100 PA**; pitchers on **WAR per 100 IP**, but with a **role-dependent innings floor — 75 IP for SP, 30 IP for RP** — rather than one shared number, since a real full-season reliever legitimately throws far fewer innings than a real full-season starter; a single shared threshold would have either excluded most legitimate relievers or admitted token/injured starters. Once both clear their own floor, they're compared on the same per-100-IP rate, which is what actually equalizes them. Both minimums also directly serve the "exclude injured/backup/token-appearance noise" goal — a small, fluky sample isn't a fair test of whether Overall predicted a player's value.
- **Fielding added as a variable, same day, Rees's follow-up.** It's a real weighted contributor to Overall (`overall = max(batting + fielding × w.fielding, pitching)`), just not a single raw grade like Contact — it's itself a derived composite, `max(catcherComposite, infieldComposite, outfieldComposite)`, each built from different raw fields plus a position-specific bonus from the active weight set. Computed in `lib/rating-validation-query.ts` the same way `lib/rating-engine.ts` computes it (not simplified), then tested as one more variable — matching how Overall itself, also a composite, is tested rather than excluded for not being a single raw grade. **First result: Fielding's R² is effectively zero (0.004)** — by far the weakest of any hitter variable. Notable, but flagged here rather than treated as a real finding yet: real-world fielding value is famously the noisiest single-season signal in sabermetrics even with far larger samples, so this could easily be one-year noise rather than a genuine weighting problem — worth specifically re-checking once more seasons accumulate, not acted on now.
- **Updated real results on the refined methodology** (455 hitters / 402 pitchers, 2031 MLB): Overall still meaningfully beats every individual grade for both types (hitters: Overall R²=0.312 vs. best single grade Gap at 0.085; pitchers: Overall 0.319 vs. best single grade Movement at 0.223) — the Movement-over-Stuff gap from the raw-WAR version held up on the harder rate-stat test too (Stuff 0.086 vs. Movement 0.223), reinforcing rather than undercutting that finding.
- Only one season of real outcomes exists so far — treat all of the above as a directional first look, not a final verdict, same framing as the market-rate curve. Gets more reliable every season as more real results accumulate, and the intent (per Rees) is to eventually tie the broader valuation model formally to season stats as that history builds up.

### Rating engine redesign, Phase 1: role-calibrated fielding weight — shipped 2026-08-31
Full proposal (5 weighting options, the anti-overinflation requirement, and the scale-recalibration plan) is a living artifact, not duplicated here — see "Rating Engine Redesign" (published to Rees, ask him for the link if it's needed and not already at hand). This entry covers just Phase 1, now built.

- **What changed**: `rating_weights.fielding` (today a single flat 0.25 applied to every hitter) is now multiplied by a per-role factor from the new `fielding_role_weights` table before being added into Overall/Potential/the H-P split — `lib/rating-engine.ts`'s three `fielding * w.fielding` sites became `fielding * fieldingWeight`, where `fieldingWeight = w.fielding * (fieldingWeights?.[role] ?? 1)`. A role with no entry, or no table at all, behaves exactly as before (multiplier 1).
- **A real reordering was required, not just an addition**: `role` used to be computed *after* `overall`/`potential` in `computeRatings()` — it was purely a display label until now. Moved the `sp_rp`/`role` block earlier (right after `pitchingP`), confirmed safe first by checking that nothing in it depends on `overall`/`batting`/`fielding` — only on raw grades and values already computed above that point.
- **How `fielding_role_weights` gets computed** (`scripts/compute-fielding-weights.ts`, wired into `refresh.ts` after `compute-team-ratings.ts`): reuses `getRatingValidationPoints()` directly (no new data-fetching) to fit a fielding-vs-WAR/100PA slope per hitter role, then runs those seven slopes through an **isotonic regression** (`lib/regression.ts`'s `isotonicRegressionNonIncreasing`, a real pool-adjacent-violators implementation) constrained to the confirmed real defensive-spectrum order (SS > CF > INF > COF > C > DH > 1B), then shrinks each role's result toward the pooled (all-hitters) slope by sample size. This is what makes "a role's fielding weight can never invert relative to another role's" a property of the method, not a hope — shrinking every role toward the *same* central reference point can only pull values closer together, never swap their order, and the isotonic step guarantees the order going in.
- **A real problem found and fixed before this ever ran for real, not after**: the first run produced roughly ×1.7-2.0 multipliers for five of seven roles — a much bigger swing than the data can support, traced to the pooled R² being only 0.004 (fielding is already the weakest predictor of any hitter grade, confirmed above). Shrinkage toward a slope that's itself statistically indistinguishable from noise doesn't fix an overinflation problem, it just relocates it — a large-sample role like INF (n=142) kept 85% of its deviation from a "reference" that wasn't a reliable reference at all. **Fixed with an explicit confidence gate**: if the pooled R² is below 0.02, every role gets a flat multiplier of 1.0 regardless of what any individual role's slope says, full stop. Confirmed this is exactly what fires today — every role currently gets ×1.00, a genuine no-op (verified: the top 5 players by Overall are bit-for-bit identical before and after this change went live). The mechanism is fully built and will start producing real differentiation on its own the moment (if) the underlying relationship strengthens with more seasons — nothing further needs building for that to happen, it just needs to clear the gate.
- **Also capped, defensively, regardless of the gate above**: `relativeMultiplier` is clamped to `[0, 3]` — fielding can never *subtract* value from Overall (a defensive specialist must never rate below an offensively-identical player with worse fielding, which a negative multiplier would do), and can never swing more than 3x today's baseline in either direction no matter what a future season's data says.

### Rating engine redesign, Phase 1 retired and replaced — decomposed offense/defense model (2026-09-01)

**Phase 1 above (role-calibrated fielding weight) is retired, not just dormant.** Measured the actual within-role variance of the two raw grades that both gate role assignment AND feed the Fielding composite: SS's own `ifr` (gate: `≥65`) has SD 3.2 within the SS bucket vs. 11.4 pooled (28% of full spread left); CF's `ofr` (gate: `≥65`) has SD 1.9 within CF vs. 9.9 pooled (19%). This is restriction of range, and it's structural, not a sample-size problem — every SS clears roughly the same `ifr` bar by definition of being called an SS, so there's almost no variance left for a within-role regression to find a relationship in, no matter how many seasons accumulate. The R² confidence gate correctly prevented it from shipping anything untrustworthy, but it was never going to clear that gate for a reason more permanent than "not enough data yet." `compute-fielding-weights.ts`/`fielding_role_weights` are left in place (harmless, still wired into `refresh.ts`, still a confirmed no-op) but should be treated as retired, not "waiting to activate." Full writeup: the "Rating Engine Redesign" artifact (published to Rees).

**Replacement: tune each value stream separately against a real, unconfounded outcome, decide the cross-component blend later** (mirrors how real WAR is built — batting/fielding/baserunning runs measured independently in a shared currency, then simply added, never regressed against each other with role as a variable).

- **Step 1, hit-tool weights vs. OPS+ (`scripts/compute-hitting-weights.ts`)**: pooled across every real MLB hitter (`league_id=200`, `mlb_service_days>0`, ≥100 PA), regresses Contact/Gap/Power/Eye/Avoid-Ks together (multivariate — `lib/regression.ts`'s new `fitMultipleLinear`, normal equations via Gauss-Jordan, no external stats library needed) against OPS+. **R²=0.496** (park-adjusted; 0.502 before park adjustment) — far stronger than any single-grade check this session found, because OPS+ isn't restriction-of-range confounded (role gates on `ifr`/`ofr`, never on hit-tool grades) and because this is the first time these grades were tested together instead of one at a time. Findings: **Contact and Power come out essentially tied** (standardized 0.521/0.521; today's weights treat them as meaningfully different, 0.375/0.325). **Eye looks overweighted today** (implied ~0.09 vs. today's 0.15). **Gap** close, maybe slightly underweighted. **Avoid Ks nets to ~0** once Contact is in the model — not a real "strikeouts don't matter" finding, Contact already bakes in Avoid-Ks+BABIP (confirmed with Rees, see `[[ootp-grade-composites]]`-style note — Contact IS a composite of exactly these). **Diagnostic only, nothing written to `rating_weights` yet** — a real decision for Rees before any of these numbers ship.
- **OPS+ didn't exist anywhere in this schema before this** (`player_batting_stats_snapshots` only has raw counting stats) — built the minimum real version needed: league-wide OBP/SLG from every real 2031 MLB PA (not just the ≥100 PA regression subset — restricting the league baseline to qualifiers would bias it upward), each player's OPS+ relative to that baseline. This is the same league-average-by-level-and-year normalization the "Not built yet" OPS+/FIP- item below already flagged — scoped here to exactly what this one regression needs (2031, MLB level only), not the general system.
- **Ballpark factors, added same day (Rees's ask, after seeing OPS+ ignored park effects entirely)**: new `ballpark_factor_snapshots` table (`refresh_run_id`, `team_id`, `average`/`doubles`/`triples`/`home_runs` + RHB/LHB splits on average and HR, capacity, stadium type, surface), captured every refresh via `scripts/scrape-ballpark-factors.ts` (wired into `refresh.ts` after `compute-team-ratings.ts`, own try/catch). Source: `https://atl-02.statsplus.net/thebigleague/ballparks/` — **confirmed public, no auth/token needed** (unlike ratings/gamehistory). **Team mapping verified directly, not assumed**: every park's `/park/{id}` URL id is identical to our own `team_id` across all 32 real teams (cross-checked against `teams.name || ' ' || teams.nickname` matching the page's `data-name` exactly) — the scraper still logs a warning if a name ever drifts, as a staleness check, but doesn't need name-matching to do the mapping. **Two real limits, both by design, not oversights**: (1) StatsPlus only publishes CURRENT factors — checked both the ballparks index and an individual park page directly, no year selector, no per-season history anywhere on the site — so this is snapshot-forward-only, accurate from 2026-09-01 on, NOT retroactively reconstructable for a relocated/renovated park from before this table existed. (2) Park attribution is per-stint (`player_batting_stats_snapshots.team_id`/`stint`, already granular enough for a traded player's stats to split correctly by team), but NOT per-plate-appearance/home-vs-road — that needs game-log data (deliberately deferred, see below) — so the standard sabermetric approximation is used instead: only **half** of a team's factor deviation from 1.0 is applied (`1 + (factor-1)*0.5`), since a player accumulates roughly half their PA at home and half spread across every other park.
- **OPS+ now applies separate Doubles/Triples/HR adjustments** (Rees's explicit ask, not one blended power factor): each stint's hits are decomposed into singles/doubles/triples/HR, each category divided by its own half-applied park factor (singles use the page's "Average" factor as the closest available proxy — OOTP doesn't publish a standalone singles factor), THEN recombined into OBP/SLG. Walks/HBP are left unadjusted (same as real-world practice — not meaningfully park-driven). League baseline stays raw/unadjusted on purpose (a real MLB league average is never itself park-adjusted either) — only individual players get adjusted, so their park-neutral rate compares fairly against that shared baseline. **Measured impact**: moves individual players' OPS+ by an average of 2.7 points, up to 11.6 for the most extreme park — real and worth having at the player level, even though it barely moved the aggregate regression weights (the park effects aren't correlated with hit-tool grades, so they wash out in the pooled fit).
- **Baserunning composite, new (Rees's ask, `scripts/compute-baserunning-weights.ts`)**: found `run` (general baserunning instincts), `steal` (stealing skill), `stlrt` (stealing tendency/aggressiveness) sitting in `player_ratings_snapshots` fully populated (113,593/113,593 non-pitcher rows, real 20-80 grades) and read by **nothing** in `lib/rating-engine.ts` before this. Regressed all four baserunning-relevant grades (these three plus `speed`) against real **UBR per 100 PA** (StatsPlus already computes UBR as a real per-season baserunning-runs stat, confirmed 647/647 populated, averaging ~0 like a runs-above-average stat should — converted to a rate the same way raw WAR was, since it's a counting stat). **R²=0.553** — even stronger than the hitting regression, a real validation that baserunning value is substantial and simply invisible to OPS+ (a stolen base isn't a hit). `run` dominates (standardized 0.556, ~72% of the implied weight) — almost certainly because it's StatsPlus's own summary baserunning grade, already blending speed/instinct/decision-making, leaving little independent signal for `speed`/`steal` alone (~5% each) once it's in the model. `stlrt` carries real independent signal beyond `run` (~19%) — how often a player's ability actually gets used, distinct from how good that ability is. **Built into `lib/rating-engine.ts`**: new `baserunning` composite (`speed*w.baserunning_speed_weight + run*w.baserunning_run_weight + steal*w.baserunning_steal_weight + stlrt*w.baserunning_stlrt_weight`, weight defaults = this regression's output), computed and stored on `player_computed.baserunning` every run, but **`w.baserunning` (its blend weight into Overall/Potential) defaults to 0 — a deliberate no-op**, per Rees: tune Batting/Fielding/Baserunning independently first, decide the three-way blend weight later. Confirmed true no-op end to end (top-5 Overall bit-for-bit identical before/after). **`speed` deliberately stays in Batting too** — not moved out — per Rees: faster batters beat out more grounders/take extra bases, a real batting-outcome effect distinct from the Baserunning composite's own value; the small overlap is accepted, not a bug.
- **Per the OOTP wiki (Rees quoted it directly)**: Speed is offense-only, does NOT affect steal ability or attempt frequency (that's what `steal`/`stlrt` are for) but does affect advancing on balls in play/scoring/stretching hits into extra bases — consistent with keeping it in Batting separately from Baserunning.

### `/admin/weight-tuning` — visualizing and tracking the three regressions above (2026-09-02)

Rees: "I want to be able to visualize and track our regressions that are currently inputting into our current weights." New report page, same pattern/visual language as `/admin/market-rates` and `/admin/rating-validation`. It computes nothing itself — it only displays what the three scripts above already found.

- **New tables**: `weight_tuning_runs` (`refresh_run_id`, `stream` — hitting/baserunning/pitching, `target_metric`, `r_squared`, `sample_size`) and `weight_tuning_coefficients` (per-variable raw/standardized coefficient, implied weight, and the **live `rating_weights` value captured at the time the regression ran** — not recomputed later, so history stays honest if the weights themselves change). Shared write helper: `lib/weight-tuning-persist.ts`.
- **All three scripts now persist, and all three are wired into `refresh.ts`** (own try/catch each, right after `compute-team-ratings.ts`) — `compute-hitting-weights.ts` and `compute-baserunning-weights.ts` previously only printed to the console and weren't in the refresh pipeline at all; both gained a `persistWeightTuningRun()` call and a live-weight lookup for the comparison column.
- **New: `compute-pitching-weights.ts`** — the pitching-side regression didn't exist before this. Target is **WAR/100 IP** (the same rate + role-dependent innings floor `/admin/rating-validation` already uses — 75 IP for SP, 30 IP for RP, pooled together), not a from-scratch park/level-neutral metric like FIP- — building a real FIP-/ERA- is separate, bigger work (see "Not built yet" below), not assumed here. Pooling SP+RP is safe from restriction of range for the same reason hitting was: role assignment there gates on `stm`/pitch-count, never on Stuff/Movement/Control/PBABIP. **First real result** (n=385, R²=0.385): Control looks overweighted today (implied 0.174 vs. current 0.300); PBABIP nets to ~0 independently, consistent with `[[ootp-grade-composites]]`'s finding that Movement already bakes PBABIP+HRA in as a composite — not a "PBABIP doesn't matter" result any more than Avoid Ks was for hitting.
- **Page**: `lib/weight-tuning-query.ts` (client-bundle-safe, same `makeSupabaseClient()`-inside-each-function pattern as the other two query layers) reads the latest snapshot per stream plus the full R²-over-time history. `WeightTuningExplorer.tsx` — stream toggle (Hitting/Baserunning/Pitching), a stat summary, a coefficient table with a simple implied-vs-current weight bar (no chart library needed, just two numbers per row), and an R²-over-time line chart across `refresh_run_id` (one point today per stream — fills in as more refreshes accumulate real history, which is the actual "track" half of the ask). Nav: Reports → Weight Tuning.

### First real weight change shipped: hitting + pitching now data-tuned, and a new cross-stream scale gap surfaced (2026-09-02)

Rees: "Let's start with hitting, and tuning those weights to align with the data." Two real fixes first, then the actual weight change:

- **Speed was wrongly excluded from `compute-hitting-weights.ts`'s regression.** Original reasoning ("OPS+ doesn't capture baserunning") was true but irrelevant — Speed is already one of Batting's own weighted inputs today, for a real non-baserunning reason Rees confirmed directly (faster batters beat out more infield hits and stretch more doubles into triples, both of which show up in OBP/SLG). Fixed: Speed is now a 6th predictor. Barely moved the other four (R² 0.498 vs 0.496) — Speed itself came back modestly important (standardized 0.051, implied weight 0.040 vs. today's 0.100, suggesting Speed is *overweighted* in Batting today).
- **Confirmed the 100 PA minimum was already applied** to the regression sample (not the league OPS+ baseline itself, which deliberately stays unrestricted — see the script's own comments) — nothing to fix there, just verified on request.
- **New `rating_weights` row (id=2, "Data-tuned hitting + pitching weights", now active)**: Batting (contact/power/eye/gap/avoid_ks/speed) replaced with the regression-implied weights above; Pitching (stuff/movement/control/stamina/pbabip) replaced with `compute-pitching-weights.ts`'s implied weights too, since fixing pitching's sum at the same time is literally what "standardize so pitchers/hitters function on a similar distribution" requires. **Both vectors now sum to exactly 1.0** (previously 1.05 for batting, 1.25 for pitching — the original PowerBI-ported values were never designed to sum to anything in particular). Old baseline kept as id=1 (`is_active=false`), not deleted — this table is a real version history, not an in-place mutation.
- **Recomputed and verified — and this surfaced a real, more-visible problem**: real rostered MLB hitters now average Overall **63.2**; real rostered MLB pitchers average Overall **52.0** — an 11-point gap on the same nominal 20-80ish scale, for players who are, by construction, equally "real MLB regulars." Fixing each stream's own weights to sum to 1.0 was necessary but not sufficient — it removed the (accidental, never-designed) partial masking that the old 1.05-vs-1.25 mismatch happened to provide, and made the underlying scale difference between hitting and pitching grades more visible, not less. **This is the direct evidence for why the top-level cross-stream standardization (proposed to Rees, not yet built) is the necessary next step, not a nice-to-have.**
- **Fielding and Baserunning's internal weights already summed to ~1** (Fielding's `cRating`/`infRating`/`ofRating` formulas were already designed this way; Baserunning was built this way from the start) — no change needed for either.
- **Proposed, not yet built**: a per-player-type calibration (separate hitter/pitcher versions of the already-designed 20-80 rescale from the "Rating Engine Redesign" artifact, each centered on its own real population average) to close the 63.2-vs-52.0 gap, plus the still-open Batting/Fielding/Baserunning top-level blend weight for Overall. Both are real decisions for Rees, not implemented unilaterally.

### Checked, not built: a Power version of the Contact/Control weak-tool gate (2026-09-02)

Rees's question, given Power's now-confirmed importance in the data: should the same nonlinear "unplayable primary tool" gate (`gate()` in `lib/rating-engine.ts`, currently applied to Contact and Control) be extended to Power? Checked two ways before building anything (`scripts/analyze-weak-tool-penalty.ts`, one-off diagnostic, not wired into `refresh.ts`):

- **Residual check on real performers**: fit the same linear hitting model, then looked at whether low-Power real MLB regulars (100+ PA) do systematically *worse* than even the linear model already predicts (which would mean a linear weight can't capture the effect — the same argument that justified gating Contact/Control). No cliff — residuals bounce around noisily across Power bins with no concentration of large negative values at the low end. Ran the identical check on Contact as a control: it *also* showed no established-regular population below grade 40 at all, which is itself informative — the gate's original justification (see `gate()`'s comment: a real SP with Control 30 ranked the league's #13 prospect) was about the **prospect pool**, not established regulars, who've already survived past having a truly unplayable tool. This makes the residual-on-regulars check subject to survivorship bias for either grade, not just Power.
- **Direct scan of real low-Power cases** (`pow <= 25`, no PA filter): no anomalous overvaluation found — every real player with a genuinely weak Power grade has an Overall that lands roughly where their other (also modest-to-average) grades would suggest, nothing resembling the "elite everything else, unplayable primary tool, absurdly high rank" pattern that justified Contact/Control.
- **Why this probably isn't a fluke of small sample, but a real baseball-logic difference**: Contact and Control are closer to *binary* gates on viability — a hitter who can't make contact never gets the chance to hit for power or use his speed; a pitcher who can't throw strikes can't pitch, period. Power is a *spectrum* — a plus-contact, average-speed, weak-power hitter is a real, viable MLB archetype (a "high-average, low-power" profile), not an unplayable one. The now-larger linear Power weight (0.414) already captures "less power costs you more value," which is the right way to represent a spectrum; a nonlinear cliff is the right way to represent a hard viability floor, and Power doesn't appear to have one.
- **Not closed forever** — worth re-checking if a future season's real data surfaces an actual anomalous case (the same way the original gates were each justified by a specific real player), but nothing in today's data supports building one now.

### Batter Overall blend: the first real signal that Fielding actually matters (2026-09-02)

Rees: "Let's start that work on Batter's Overall" — Step 3 of the decomposed redesign, finally buildable now that Batting/Fielding/Baserunning are each individually tuned. New `scripts/compute-overall-blend-weights.ts`, new `overall_blend` stream on `/admin/weight-tuning` ("Batter Blend" tab).

- **What it regresses**: NOT raw tool grades this time — the three already-computed composites (`player_computed.batting`/`fielding`/`baserunning`, each individually tuned and roughly on the same 20-80ish scale) against real WAR/100 PA, pooled across every real MLB hitter, no role split. Same "pooled, not role-bucketed" design as everything else this session.
- **Real result** (n=436, R²=0.320): Batting standardized 0.598 (implied 56.7%), **Fielding 0.286 (implied 27.1%)**, Baserunning 0.170 (implied 16.1%).
- **This is the first time Fielding has shown a real, substantial relationship to value all session** — the original pooled single-variable check (way back near the start of this whole redesign) found Fielding's R² against WAR to be ~0.004, functionally zero. The likely explanation: that check was confounded by a **suppressor effect** — better fielders tend to be worse hitters in this league (real positional value already reflects that tradeoff, e.g. glove-only middle infielders vs. bat-only 1B/DH types), so Fielding's simple correlation with WAR was masked by its negative correlation with Batting. Once Batting is properly controlled for in a real multivariate fit, Fielding's independent contribution becomes clearly visible. This is a textbook case of why a multivariate regression can find a real relationship a univariate one hides — not evidence the earlier 0.004 finding was wrong, just incomplete.
- **Today's actual weights are badly out of line with this**: Batting is implicit weight 1 (nothing multiplies it in `overall = max(batting + fielding*fieldingWeight + baserunning*w.baserunning, pitching)`), Fielding is a flat 0.25 (`rating_weights.fielding`, historically picked just to keep the sum from swamping Pitching's differently-scaled sum, never derived from data), Baserunning is 0 (deliberately deferred). That's (1, 0.25, 0), summing to 1.25 — not 1, and Baserunning getting literally zero credit despite the data saying it should carry real weight (16%).
- **Diagnostic only, same discipline as every other regression this session** — nothing written to `rating_weights` yet. Wired into `refresh.ts` (own try/catch, after `compute-pitching-weights.ts`) so it accumulates real history like the other three.

### Not built yet (don't design against these)
- Win/loss-based team power rankings (needs league-relative stat normalization, not started)
- Ratings/computed values for pre-draft amateurs — same rating engine works fine on them once they have data, but the ingestion pipeline for their scouted grades is still manual/deferred
- Team financials beyond raw contract data — team payroll is **not** simply `SUM(contracts.salary0)`; retained salary on trades breaks that naive sum and hasn't been solved yet. **See the transaction-analysis section above — `/thebigleague/finance/current/` may solve this directly, not yet checked.**
- Per-game box scores / play-by-play — confirmed possible via scraping StatsPlus's static HTML reports, deliberately deferred until after core parity
- Player acquisition method (drafted/traded/int'l signing/waivers/rule 5/scouting discovery) — confirmed 2026-08-19 that StatsPlus tracks this cleanly (an "Entry" field + full trade history per player) but only on individual player profile pages, no bulk endpoint. Scraping all 45,684 players isn't reasonable (~19 hours at the existing polite throttle); deliberately held off without building a partial version pending a scoping decision (e.g. top prospects only, or one org's roster). **Superseded for the trade-history half 2026-08-31 — see above: the 276 `was_traded=true` players are scraped and shipped (`trade_events`/`trade_event_items`). The broader "Entry" acquisition-method field (drafted/int'l/waivers/etc. for every player, not just trades) is still the full 45,684-player problem, untouched.** See `statsplus-api-inventory.md`'s "Player profile pages" section for the full findings.

## 5. Existing front-end code (starting point, not gospel)

Minimal Next.js 14 App Router, explicitly built with "no polish, just functional tables" as the brief — expect to replace most of the visual layer.

- `/players` — top 100 by Overall, org filter
- `/prospects` — the most developed page, substantially reworked 2026-08-19 into a real "publish to Slack" report. Two rows per player: a compact main row (logo spanning both rows with a border on 3 sides only — no border where it meets the Org column — Org abbr, Rank, Name (bold), Role [`player_computed.role`, a projected defensive grouping like SP/INF/OF/UTIL, not raw `pos`], Age, LVL, ETA, Org Rank [`prospect_org_rank`, not the plain `org_rank`], Draft Yr/Rd/Pick) plus a borderless detail row underneath: a stat line at the player's **current level only** (no more by-level breakdown or summing across levels, since 2026-08-19) — pitchers get IP/FIP/ERA/K-9/WAR, batters get AB/the standard AVG-OBP-SLG slash line/HR/SB/ZR/WAR — followed by a scouting blurb. The blurb prefers a stored `prospect_bios` row (hand/AI-written, per `prospect-bio-style-guide.md`, with a ⚠ shown if the stored bio is older than the current snapshot) and falls back to the auto-generated `gradeWord()` one-liner in `ProspectTable.tsx` for anyone not yet covered. FIP uses a fixed placeholder constant (3.10, not derived from this league's real run environment — see the note at `FIP_CONSTANT` in `lib/queries.ts`) until the league-average-by-level-and-year normalization work happens. ZR comes from `player_fielding_stats_snapshots`, newly joined in for this — that table uses `split_id=0` for "overall," not `1` like batting/pitching (gotcha 14). Table is `width: auto` + fully left-aligned (see `.prospect-table` in `globals.css`, scoped so it doesn't affect other pages) so columns are only as wide as their content, not stretched to fill the container. Has a "Change from" baseline picker (`?since=<refresh_run_id>`, populated by `getProspectSnapshotOptions()`, snapshots with no recorded game date excluded) showing ▲/▼ deltas on Overall/Potential/Prospect Potential/ranks against any past `player_computed` snapshot — round-then-diff, not diff-then-round (see `ProspectDelta` comment in `lib/queries.ts`). As of 2026-08-20 it's a `"use client"` component (Hitters/Pitchers toggle + multi-select Role chips, both filtering the already-fetched rows in-browser, no page reload) — **display helpers it needs (`levelLabel`/`teamLogoUrl`) must come from `lib/display-helpers.ts`, never `lib/queries.ts`, or the page crashes in the browser** (gotcha 16). Stat-line numbers read "value LABEL" (`26 AB`, not `AB 26`); FIP/ERA always show 2 decimals, everything else 1. Player names link out to their StatsPlus page (`https://atl-02.statsplus.net/thebigleague/player/{player_id}/`, new tab). Rank and Org are their own `rowSpan={2}` anchor columns (large bold font, top/bottom border, background matching the logo cell) that come *before* the logo, matching Logo's own styling — this replaced an earlier layout where Rank/Org were plain single-row cells. Sits in a flex row next to a second table, `TeamRankingsTable.tsx` (`getTeamRankings()` in `lib/queries.ts`) — one row-pair per org, **restructured 2026-08-20 to Rank → Logo → Team** (both Rank and Logo `rowSpan={2}` with the same anchor-cell treatment as the prospect table, replacing the original Team/Rank/logo-on-the-right layout) showing Minors Rank/Pitch Rank/Bat Rank/Readiness Rank/# of leaguewide top-100 prospects (detail row reads simply "`N` Top 100", no "prospect(s)" wording). `page.tsx` has a bare spacer `<div style={{ height: 32 }} />` above `TeamRankingsTable` to compensate for `ProspectTable`'s own internal H/P + Role filter-bar div, which it has and `TeamRankingsTable` doesn't — without it the two tables' actual `<table>` elements start at different Y-offsets even though their outer flex columns are top-aligned; 32px was empirically measured (via a live DOM `getBoundingClientRect()` diff in a fresh tab, not guessed) to land both tables' top edges on the exact same pixel. If either table's filter/header chrome changes height again, re-measure and adjust that number rather than assuming it still holds. Those four ranks are **not new methodology** — they're `team_computed.minors_rank`/`batting_prospect_rank`/`pitching_prospect_rank`/`tbl_readiness_rank`, a straight port of the original Power BI "RLB" system already being computed by `compute-team-ratings.ts` (avg Prospect Potential of the org's top-20/10-hitters/10-pitchers by `prospect_org_rank`; readiness is avg *current* Overall of that same top-20 pool, not potential — how close the system already is, not its ceiling).

**Further 2026-08-20 follow-ups, same session:** Both tables now have an `<h2>` section title above them (`sectionTitleStyle` in `page.tsx`) — "Top Prospects" and "System Rankings" — identical style/height on both so the 32px spacer's alignment math (above) still holds; the spacer moved to sit *after* the title on the `TeamRankingsTable` side, not instead of one. `ProspectTable`'s Rank column is now center-aligned (header and cell); its Org column font dropped from 0.9375rem to 0.8125rem so it reads more clearly smaller than Rank. Players with no draft record at all (`draft_year`/`draft_round`/`draft_overall_pick` all `null` — international signees, per Rees) show italic "INT" in the Draft Yr cell with Rd/Pick left blank, instead of three dashes. `TeamRankingsTable`'s detail row (`Pitch #.. · Bat #.. · Readiness #.. · N Top 100`) got `whiteSpace: "nowrap"` — without it the browser was shrinking that line to fit whatever width the flex row squeezed the table into, since `.prospect-table` is `width: auto`; forcing nowrap makes the table grow to fit it instead (same escape-hatch horizontal scroll as before if the window's too narrow for that). Team names in `TeamRankingsTable` now link to `/prospects?team={team_id}` (carrying `since` through too, if a baseline comparison is active) so clicking a team filters the Top Prospects table beside it. **Deliberately a plain `<a>`, not `next/link`'s `<Link>`, for two independent reasons** — worth remembering before "helpfully" switching it back: (1) `TeamRankingsTable.tsx` is a Server Component, and Next.js hard-errors ("Event handlers cannot be passed to Client Component props") if a Server Component hands `<Link>` inline function props, which the original hover-underline implementation did; (2) even after that was fixed by moving the hover state to a CSS `:hover` rule (`.team-name-link` in `globals.css`), `<Link>`'s client-side transition doesn't remount `ProspectFilters`, and that component's Organization `<select>` uses `defaultValue` (only honored at mount) — so the URL and prospect table would filter correctly while the dropdown kept showing "All teams", a real state-desync bug caught by checking `orgSelect.value` after a live click in a fresh tab, not just eyeballing the page. A full-page `<a>` sidesteps both issues and matches how every other filter interaction on this page already works (`ProspectFilters` submits a real GET form).

**List size expanded 100 → 500, same session (2026-08-20).** `TOP_PROSPECTS_LIMIT` in `lib/queries.ts` (used by `getTopProspectsDetailed`, `/prospects`'s data source) — the rating engine already ranks the *entire* eligible prospect pool (9,266 players as of refresh_run 10), so ranks 101-500 were real, already-computed data with zero backend work needed; `fetchComputedPlayers` also already pulls the full player universe before slicing regardless of the limit number, so this added no measurable query cost (confirmed: warm server-render time was ~same before/after, ~900ms). **Bio coverage: expanded from top 100 → top 200, 2026-08-24.** All 200 bios were rewritten from scratch against current (post-2026-08-24-refresh) data, following `prospect-bio-style-guide.md` as-is — Rees explicitly dropped an earlier idea of also working rank/grade deltas into the prose, so these are straight re-applications of the existing style rules, not a new format. Written and verified in four 50-player SQL batches (`prospect_bios`, upserted by `player_id`, all tagged `refresh_run_id` 11); every batch was mechanically length-checked (≤140 chars, zero exceptions in the final versions) before going anywhere near the database, per the character-count lesson in the style guide itself. A player who drops out of the top 200 keeps their old bio untouched (never deleted, just no longer refreshed) — confirmed live: 2 such stale entries remained visible after the rewrite, both still showing their correct old text and `(stale since ...)` label. Ranks 201-500 still show their stat line with no bio text at all (no auto-generated fallback) until/unless more get written — that "no fallback" rule itself is unchanged. `TeamRankingsTable`'s "N Top 100" stat is unaffected by this — it's queried independently against a hardcoded `prospect_rank <= 100` filter in `getTeamRankings()`, not derived from the main list's limit, so it stays meaningful as a fixed "elite prospect" cutoff regardless of how deep `/prospects` itself goes (also Rees's explicit call). **One real, measured trade-off: client-side filtering got noticeably slower.** `ProspectTable` renders 2 `<tr>` per player (1,000 DOM rows now, vs 200 before) and re-filters via the H/P/Role buttons in-browser (`"use client"`, gotcha 16). Measured via a `MutationObserver` round-trip in a live browser tab (not guessed): a filter-button click now takes **~330ms** from click to the DOM actually updating, vs near-instant at 100 rows. Not broken, but a perceptible delay — worth knowing about if it comes up again, and a candidate for pagination/virtualization if it ever feels bad enough to fix.

**`TeamRankingsTable` restructured again, same session (2026-08-20 follow-up).** Pitch/Bat/Readiness/Top 100 -- previously packed into the detail row as text -- are now their own real columns on the main row (`COLUMN_COUNT` went 3 → 7: Rank, Logo, Team, Pitch, Bat, Readiness, Top 100). The detail row instead highlights the org's own top 3 prospects, each rendered `**#Rank ROLE** Name` (rank+role bolded, name plain), joined by " · " — e.g. `#1 SP R.J. Blum · #34 1B Fernando Jaquez · #42 INF Carlos Bueno`. New data comes from `TeamRankingRow.topProspects` in `getTeamRankings()` (`lib/queries.ts`): a `player_computed` query filtered to `prospect_org_rank <= 3` (org-relative, so no extra per-team scoping needed — unlike the existing top100Count query, which filters the *leaguewide* `prospect_rank` and does need every org's `team_id`), joined to `players` for name/`organization_id`, grouped and sorted client-side into a 0-3-entry array per org. **`prospect_org_rank` selects and orders which 3 players (and in what order) — `prospect_rank` (leaguewide) is what's actually displayed as the number**, per Rees's same-session follow-up ("should be the prospect's overall rank, not just 1,2,3"). Both are fetched per row and kept separate (`orgRank` drives `.sort()`, `rank` — the leaguewide one — is the only one that reaches `TeamRankingRow`) specifically so this distinction doesn't get collapsed back into one field by a future edit. Confirmed live against the Top Prospects table itself: org 24's #1 org prospect (R.J. Blum) is leaguewide #1 too (coincidence of him being the true #1 overall), but its #2/#3 org prospects show #34/#42, not #2/#3. Confirmed live: all 32 orgs currently have a full 3 entries (no org is short enough to hit the "No ranked prospects" empty-state text, though that path exists defensively). Table-alignment spacer (32px, described above) is unaffected — this only changed column *count*, not header/filter-bar *height*.
- `/draft` — top 100 draft-pool prospects by Prospect Potential, scoped to the latest `draft_class_imports` row
- `/org-minors` (added 2026-08-19, backend session, quick turnaround for a live in-game roster decision) — one org's full farm system: MLB + international academy + all 6 minor-league affiliates, one independently-scrollable table per team in a 2×4 grid, position counts per team up top, Hitters/Pitchers/All filter, sortable columns, Ovr/Pot shown at full precision with a red→orange→yellow→green→light-blue font-color gradient (no rounding — this is Rees's own org, the rounding rule in gotcha 9 is about hiding data from *other* GMs). Defaults to org id 15 (Oklahoma City), override with `?org=<team_id>`. Its own query layer is `lib/org-minors-query.ts`, kept separate from `lib/queries.ts` on purpose to avoid touching a file the front-end session might be mid-editing. This is the page that surfaced gotcha 11 below.
- `/glossary` (added 2026-08-24, expanded to full coverage same day) — Rees's own reference doc for every calculation, categorization, and ranking system the app makes, written up in plain language with real formulas, not just a summary. **Deliberately full-precision throughout — the public-facing rounding rule (gotcha 9, "never show Overall/Potential/Prospect Potential unrounded") does NOT apply here.** This page is internal-nav-only, never part of the nav-less `/report` external treatment, specifically so it's safe to leave unrounded. Eight sections, **in this order top to bottom deliberately** (the two weight-testing tables lead the page, per Rees — everything else follows below them): Role Representation (a weight-testing diagnostic, see below), the Role × Level Overall Benchmark table, Overall & Potential (Batting/Fielding/Pitching/QP-QPP formulas, plus a live table of whatever weight set is currently active in `rating_weights` — pulled fresh via `getActiveWeightSet()` in `lib/queries.ts`, so the page can't drift out of sync with a weight-set change), Prospect Potential (the bust-risk discount + blend formula), Role & Classification (the Role priority table, SP/RP, TBL Pos, Platoon), Rankings (all 5 rank fields and the prospect-pool eligibility rule), ETA, and Minor League System & Team Rankings (all 5 `team_computed` metrics, sourced from `compute-team-ratings.ts`, including the one — Roster Rank / Team OVR — that's computed and stored but not actually shown on any other page yet). **Expect this to keep growing** — it's meant to be the permanent record every time a formula changes, not a one-time writeup.

**Role Representation diagnostic, added 2026-08-24, widened and moved same day (`getRoleRepresentation()` in `lib/queries.ts`, `ROLE_REP_LIMIT` in the page).** Two tables — role counts in the top N by Overall, and by Prospect Potential, N=200 (widened from an initial 100 the same day for a steadier sample) — each with a baseline comparison column (that same role's share of the full reference population: every ranked player for the Overall table, the prospect pool specifically for the Prospect Potential table, since `prospect_rank` is only ever assigned within that pool) and an Index (top-N share ÷ baseline share × 100 — 100 is proportional, meaningfully above/below flags a role the weights may be pulling up or down). Purely a read query over `player_computed` — no new computed/stored fields, nothing for `compute-ratings.ts` to write. **This table and the Role × Level Overall Benchmark table were both moved to the very top of the page the same day** (Rees's call — they're the two tables he actually wants to look at first when testing weight changes; everything else on the page stayed in its original order below them, and the few in-page cross-references that used to say "table below" for these two had to flip to "table above"/"further down this page" accordingly). **SS index runs ~850-1200, DH ~7-20** — confirmed with Rees this is *expected*, not a red flag: SS's Role bucket threshold (`pot_ss ≥ 55 AND ifr ≥ 65`) is deliberately narrow because genuinely few players can play competent shortstop defense at all, so of course the ones who clear it skew elite; DH is the opposite, the pure fallback bucket for anyone who cleared no position-fit bar, so it's structurally full of marginal profiles. Documented directly on the page now (not just here) so this doesn't get re-litigated as a bug next time someone looks at the table. The real signal to chase is a role whose bucket definition *isn't* unusually narrow or wide but still shows a large Index.

**ETA reworked from a flat global bar to role-aware, then twice more the same day (2026-08-24, Rees's spec each time).** The original formula (`estimateEta(overall)` in `compute-ratings.ts`) was a step function purely off how far a player's Overall sat below a single fixed 65 bar, same for every position. **Current version** (see gotchas 19-20 for the two intermediate steps and exactly what each one fixed): for each of the 9 `role` buckets, the real average Overall of every player currently at each level (1=MLB through 6=Rookie, plus a synthetic 7=International tier) becomes that role's own benchmark ladder, replacing the old flat 65 for everyone. A prospect gets **no ETA at all** if they're no longer prospect-eligible, or if their Potential never clears their own role's MLB bar (the level-1 rung of that ladder). Everyone else: their **current Overall is interpolated onto that same ladder** to find a "Suggested Level" — where their actual ability already sits, entirely independent of what level they're rostered at, with no roster-level shortcut anywhere (not even for someone literally on the active MLB roster today). That fractional distance-to-the-majors is converted to years using a pace set by how far their **Potential** (not Prospect Potential — deliberately the raw risk-adjusted ceiling, not the ranking-blended metric) clears the bar: 15+ margin = 0.8yr/level (fast-tracked), 8-15 = 1.0yr/level (normal), 0-8 = 1.3yr/level (fringe, full runway). Recomputed fresh every refresh, same as the rest of the rating engine — not a persisted/cached table. Hand-verified against multiple real players at each of the three versions, including the two edge cases that drove the reworks (a player outperforming his roster level, and one underperforming his). Full writeup lives on `/glossary` itself, not duplicated here — kept in sync with the code every time the formula changes.

`lib/queries.ts` has the working query patterns (`getTopPlayers`, `getTopProspects`, `getTopProspectsDetailed`, `getTopDraftees`, `getOrgTeams`) plus small but load-bearing helpers: `levelLabel()` (numeric level code → "AAA"/"AA"/etc.), `teamLogoUrl()` (constructs StatsPlus's logo image URL from team name — verified working for a few teams, not exhaustively), and `roundGrade()` (see gotcha 9 below).

**As of 2026-08-19, the front-end session has started actively iterating on the front end** (new nav component, styling, page structure) — if you're a backend session reading this, expect `app/**` to look different from the description above; that's expected, not a sign something broke. Don't "fix" front-end files back to match this doc.

**Backend-session exception, 2026-08-24**: Rees asked directly for a name search box on `/prospects`' Top Prospects table, so it was implemented here rather than routed to the front-end session — `app/_components/ProspectTable.tsx` gained a `search` state + input, purely client-side, filtering on top of the existing H/P and Role filters (matches against `"${first_name} ${last_name}"`, case-insensitive substring). Scoped to that one table only, per Rees's spec — `TeamRankingsTable` alongside it is untouched. If you're picking this file back up on the front-end side: this is a small, self-contained addition following the file's existing filter-bar pattern, not a sign of a broader front-end takeover — flagging here so it's not a surprise.

**Same exception, same day, three small cosmetic tweaks**: `ProspectFilters.tsx`'s "Change from" dropdown now shows the bare date (`2031-07-21`) instead of `"Game date 2031-07-21"`. `ProspectTable.tsx`'s Rank/Logo cell border and `TeamRankingsTable.tsx`'s Logo cell border were each dropping one side (`logoCell`'s `borderLeft` in the former, `borderRight` in the latter) that was rendering as an unwanted line next to the Rank column / Logo column respectively — both removed. All three are the kind of thing the front-end session would normally own; noted here for the same reason as the search box above.

**Same exception, same day, one real feature**: `ProspectTable.tsx`'s bio text (in the detail row, under the stat line) is now a per-row collapsible dropdown, folded by default — a player has to click a `BIO ▼`/`▲` inline text toggle to read it, matching the search box's precedent for "Rees asked directly, so built here rather than routed to the front-end session." New `expandedBios: Set<number>` state, keyed by `player_id` (not row index, so it survives the filtered/sorted set reshaping). **Debugging note for future verification passes**: while confirming this worked, a same-script `element.click()` immediately followed by reading `aria-expanded` in the same `javascript_tool` call kept reporting stale (pre-click) state — not a real bug, just React 18's batched/async state commit not having flushed yet within one synchronous script. Splitting the click and the read into two separate tool calls (a real round-trip) showed the true state correctly. Don't mistake this pattern for a broken click handler in future testing.

## 6. Gotchas already discovered (save yourself the rediscovery time)

1. **No foreign keys between `player_computed`/`player_ratings_snapshots`/stats snapshot tables.** They're sibling tables independently keyed by `player_id` + `refresh_run_id`, not parent/child. Supabase's automatic embedding (`select("*, other_table(...)")`) **will fail** with "no relationship found" between any two of these. Fetch separately and join in JS (see `fetchComputedPlayers` in `lib/queries.ts` for the pattern) or write raw SQL.
2. **Supabase caps a single `.select()` at 1000 rows by default.** Always paginate with `.range(from, to)` in a loop when you might have more than 1000 matching rows (players table alone is ~45,000 rows). See `fetchAll()` in `lib/queries.ts`.
3. **Import extensions matter and differ by context.** Files under `scripts/` run via `tsx`/Node's ESM loader and **require** explicit `.js` extensions on relative imports (`from "./foo.js"` even though the file is `foo.ts`). Files under `app/` and anything only consumed by Next.js's webpack bundler (like `lib/queries.ts`) **must not** have the extension (`from "./foo"`) or the build fails with "Module not found." This split is intentional, not a mistake to "fix" — don't add `.js` back to `lib/queries.ts`'s imports.
4. **Next.js dev server caches Supabase responses across external database changes.** If the backend session runs a refresh/recompute script while your dev server is running, the page will keep showing old data. `rm -rf .next` and restart `npm run dev` to see fresh data. There's no code fix for this yet — just a routine to remember.
5. **League level codes need decoding, and it's not obvious from the number alone.** Confirmed empirically: `0`=unassigned, `1`=MLB, `2`=AAA, `3`=AA, `4`=A+, `5`=A-, `6`=Rookie. Use `levelLabel()` rather than showing the raw integer.
6. **A "prospect" is not simply "anyone with `prospect_rank` set who has no org."** Free agents only belong in prospect rankings if they have a real `last_team_id` (previously rostered — a "true" free agent). Free agents with no `last_team_id` are almost always amateur draft-pool players who've never been rostered, and the *vast majority* of those are years away from being relevant (future draft classes). This filter already lives in `compute-ratings.ts` (backend), so `player_computed.prospect_rank` is already correct — just don't re-derive "is this a prospect" from raw `players` fields yourself; trust `prospect_rank is not null`.
7. **Stats endpoints are split by league/level — a season can have multiple rows per player.** See §4. `getTopProspectsDetailed`'s `seasonStints` array is the pattern to follow if you need per-level season stats anywhere else.
8. **StatsPlus's `lid` parameter is a trap, in two opposite directions.** On `playerbatstatsv2`/`playerpitchstatsv2`/`playerfieldstatsv2`, *omitting* `lid` silently scopes results to MLB only (`lid=200`) — there's no "all levels" shortcut, so the refresh script loops over every level's `lid` (200/201/202/203/204/205/206 = MLB/AAA/AA/A+/A+/A-/Rookie) per year. This bit us once already — a version of the refresh script that never passed `lid` looked completely successful (200 OK, real rows, no errors) while quietly capturing zero minor-league stats. On `teambatstats`/`teampitchstats`, it's the opposite: `lid` is silently *ignored*, and those endpoints only ever return MLB teams no matter what you pass — looping `lid` there just re-fetches identical rows (and will violate a DB uniqueness constraint on the second pass). See `platform/statsplus-api-inventory.md` for the full writeup.
9. **Overall/Potential/Prospect Potential are rounded to the nearest 5 for display, everywhere a reader outside this org could see it.** This is deliberate (Rees doesn't want other league GMs reverse-engineering exact scout ratings from a Slack report or public page) — use `roundGrade()` from `lib/queries.ts` for these three fields specifically in any new report or page. The underlying tool grades (Cntct/Pow/Stf/etc.) are shown at full precision for now but are planned for removal later too — don't build anything that assumes they'll always be visible. Internal/database values stay full-precision regardless (ranking still needs the real numbers) — this is display-only rounding, never applied before storing or ranking.
10. **A genuinely empty response body from a StatsPlus endpoint is a valid zero-rows result, not an error.** Only the literal string `"Unknown API"` means something's actually wrong (bad endpoint name/params). A blank response can legitimately mean "this level/year combination had no games logged yet."
11. **International-complex / not-yet-formally-rostered amateur signees have no team of their own — they hide inside the MLB roster, and `level` alone can't tell them apart.** Discovered 2026-08-19 building `/org-minors`. StatsPlus has no separate team entity for an org's international academy: those players are ordinary `players` rows with `team_id` = the org's own MLB team and `level = 1`, identical to real MLB players on paper. The only tell is **`league_id`**: real MLB roster players carry `league_id = 200` (same value as the `LEAGUE_IDS` used for stats pulls); international/complex signees carry a **negative** `league_id` — confirmed `-200` for org 15 (Oklahoma City): 34 players at `league_id = 200` (ages 22–33, a normal active roster) vs. 60 at `league_id = -200` (ages 16–19, confirmed against a real player Rees named). If you're building anything that means "the real MLB roster" or "the international group," filter on `league_id`'s sign, not on `level`/`team_id` alone, or you'll either drop these players silently or lump them in with the actual MLB roster. Working implementation: `lib/org-minors-query.ts`.
    - **Confirmed league-wide 2026-08-31, and found a real (small) crack in it while building the Overall-rescale proposal.** Checked every org's `league_id ∈ {200, -200}` age distribution directly: the split holds cleanly almost everywhere — every org shows a clean ~17-18-year-old cluster at `-200` and a ~27-29-year-old cluster at `200`. But it's not perfectly clean: **7 individual player records (concentrated in a few orgs, e.g. org 6) are mislabeled straight into `league_id = 200` despite being 17-20-year-old international-type profiles** — confirmed by their `league_id = 200` min age (17-19) sitting far below every other org's `200`-bucket minimum (21-27). This isn't a systemic convention failure, just a handful of individual data-quality stragglers — but it's exactly the kind of thing that silently poisons any calculation treating `league_id = 200` alone as "real MLB players": it was found because it dragged a real-MLB-average-Overall calculation's minimum down to 26.9 (nearly 6 SD below the mean).
    - **Fix refined the same day, Rees's correction: age is a proxy, not the real identifier — `mlb_service_days` is.** An `age ≥ 20` guard worked (dropped exactly the 7 stragglers, all confirmed `mlb_service_days = 0`), but Rees correctly rejected age (and, separately, draft status — plenty of real MLB talent signs internationally rather than through the draft, so "went through the draft" isn't a valid proxy for "is real MLB talent" either) as the wrong signal to hang this on: nothing about being young or undrafted actually means "hasn't cracked an MLB roster," it just happens to correlate with it in this specific straggler case. **`mlb_service_days > 0`** is the direct, correct identifier — literally "has this player ever accrued real MLB roster time," independent of age, draft status, or signing route. Verified it's strictly better: filtering on it instead of age caught *more* contamination (13 records, not 7) and produced a cleaner reference population (min jumped to a believable 49.9, SD tightened further to 5.2). Checked the obvious failure mode before trusting it — a genuine rookie debuting this season with real playing time but zero prior service days — and found none: every player with 50+ real 2031 PA already shows accrued service days by this point in the season. **The corrected filter is `league_id = 200 AND mlb_service_days > 0`.**
    - **Scoped deliberately: this belongs in reference-population/statistical calculations, not on `/org-minors`.** Confirmed with Rees directly — that page's whole job is showing the complete org, international signees included, so filtering any of them out there would be wrong, not a fix. The `mlb_service_days > 0` guard is for anywhere computing a statistic meant to represent "real MLB talent" (the Overall-rescale work); it is **not** a blanket rule for every `league_id = 200` usage in the codebase.
12. **A dev-server restart alone does not clear Next's stale-data cache — you need `rm -rf .next` too.** Confirmed 2026-08-19: ran `compute-ratings.ts` against a new refresh run while the dev server was up, killed and restarted the node process, and `/prospects` kept showing the *previous* run's numbers for `latestRefreshRunId()` regardless — only actually fixed once `.next` itself was deleted before the restart. Gotcha 4 above already warned about this category of staleness; this is the concrete confirmation that a process kill isn't sufficient on its own, the on-disk cache directory has to go too.
13. **Any Supabase `.range()` pagination without an explicit `.order()` has no stability guarantee — this was silently producing a different "top 100" on every page load.** Discovered 2026-08-19 chasing what looked like a broken delta calculation: `fetchComputedPlayers`'s base `players` query (`lib/queries.ts`) had no `.order()`, so Postgres/PostgREST was free to return rows in a different order across separate requests. For the unfiltered ~45k-row case this also risked genuine duplicate/missing rows across the multi-page `fetchAll()` loop, not just reordering. Fixed by adding `.order("id")` everywhere a `fetchAll()` loop scans more than one page worth of rows (see the comment at the fix site in `lib/queries.ts`) — if you add a new `fetchAll()`-based query over an unfiltered or lightly-filtered table, add an explicit order or you'll eventually hit the same bug.
14. **`player_fielding_stats_snapshots` uses `split_id=0` for "overall" — batting and pitching use `split_id=1`.** Confirmed 2026-08-19: 100% of fielding rows (65,535/65,535) are `split_id=0`. Filtering fielding by `split_id=1` (matching the batting/pitching convention) silently matches zero rows — no error, just every fielding-derived field (e.g. ZR) blank for every player. Caught by checking a known player's raw fielding row directly rather than trusting the empty result. If you add a new query against this table, don't assume it shares the batting/pitching split convention.
15. **A player can have more than one stat row at the SAME level in the SAME season** (optioned down and recalled to the same level, etc. — confirmed real, e.g. player 36509 had two separate level-4 batting rows in 2031, `stint` field `0` on both so it doesn't even distinguish them). `getTopProspectsDetailed()`'s current-level stat lookup originally used `.find()` to grab a single matching stint, silently undercounting the season by however much the other stint(s) contributed. Found 2026-08-20 while pulling data for the prospect bios, fixed the same day by summing every stint at the current level (averaging for ZR, since that's a rate stat, not a counting one) instead of taking the first match. If you write a new query against any `player_*_stats_snapshots` table filtered to "current level," sum/aggregate across matches — don't assume one row per player-level-year.
16. **A `"use client"` component that imports even one *value* (not type) from `lib/queries.ts` crashes in the browser with `"SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY must be set"`.** `queries.ts` creates its Supabase client at module scope (`const supabase = makeSupabaseClient()`, runs immediately on import), and those env vars are server-only secrets never sent to the browser — so the moment `queries.ts` gets pulled into the client bundle, that line throws on load. `import type { X } from "./queries"` is safe (erased at compile time, never bundled); a plain `import { levelLabel } from "./queries"` is not. Hit this 2026-08-20 turning `ProspectTable.tsx` client-side for its H/P/Role filters — it had been importing `levelLabel`/`teamLogoUrl` as values from `queries.ts`. **Fix, and the actual rule going forward:** pure, no-Supabase-dependency display helpers (`roundGrade`, `levelLabel`, `teamLogoUrl`) now live in `lib/display-helpers.ts`, which `queries.ts` imports and re-exports (so existing server-side `from "./queries"` imports are unaffected) but which any client component should import *directly* instead. Any new pure display helper a client component might need goes in `display-helpers.ts`, not `queries.ts` — check this before adding one. Also cost real debugging time because the failure looked like a stale-cache issue at first (this session's console-log tooling appears to persist old errors across navigations within the same tab — a brand-new tab showed the real, current state cleanly and settled it).
17. **Gotcha 12's stale-cache warning applies even when NOTHING but database rows changed — no code edit at all.** Hit this concretely 2026-08-24: rewrote all 200 `prospect_bios` rows directly via SQL (`execute_sql`, not through the app), then checked `/prospects` on the already-running dev server (`export const dynamic = "force-dynamic"` was in place, no code touched) and it kept serving the *old* bio text and the *old* `bioStale`/`bioDate` values — confirmed via direct `curl` against the dev server, not just the browser, so it wasn't a browser-cache issue either. Only resolved by the full standard procedure: kill the process, `rm -rf .next` (twice), restart, re-verify with `curl` before trusting the browser. Lesson: `force-dynamic` is not a reliable guarantee against Next's Data Cache for *every* fetch a Server Component makes (here, the Supabase client's underlying `fetch` calls) — after any direct-SQL data change on a page you're about to check live, do the full restart+wipe ritual pre-emptively rather than assuming force-dynamic alone will show fresh data.
18. **`players.is_active` is a level-1-only concept — every level 2-6 row has it `false` unconditionally.** Confirmed by direct query 2026-08-24 while building the role-aware ETA model: "active roster" isn't something OOTP tracks for minor-league rosters, only the majors. Filtering *any* query by `is_active = true` across every level (rather than gating it to `level = 1` specifically) would silently return zero rows for every minor-league level. Found because the naive "average Overall of every level-1 player" for a role was including ~1,400 non-active level-1 rows (DFA'd players, and international/complex signees mistagged at level 1 — see the org-minors gotcha below) alongside the ~890 real active-roster ones, badly understating several roles' real MLB bar (SP/RP/DH/COF's apparent "MLB average" was reading *below* their own AAA average before the fix). If you write a new query that should mean "the active 26-man roster," add `is_active = true` scoped to `level = 1` only — never as a blanket filter.
19. **International/complex signees need their own explicit remap wherever `players.level` drives real logic — they don't have a level code of their own.** Direct fallout of the gotcha above and the org-minors one before it: after gating the MLB benchmark row to `is_active = true`, international signees (level=1, negative league_id) weren't polluting that row anymore, but they'd also become entirely invisible to the Role × Level benchmark table AND — a real correctness bug, not just a display gap — an individual international prospect's OWN `estimateEta()` call was reading their raw `level` as 1 and short-circuiting straight to "already at the majors" (this specific shortcut no longer exists at all as of gotcha 20 below, but the fix here — the `effectiveLevel` remap — is still current and still needed for the benchmark table and the sanity-gate check). Fixed 2026-08-24 by adding a shared `effectiveLevel(level, league_id)` helper (duplicated in both `lib/queries.ts` and `scripts/compute-ratings.ts` — matches the `fetchAll` duplication pattern already established between those two) that remaps `level=1 AND league_id<0` to a synthetic level 7 ("International," one rung below Rookie), used everywhere level actually matters. Verified against real players afterward — the same international signees now show ETAs 6-8 years out instead of the current year. **The lesson generalizes**: any new code that reads `players.level` directly (not through `effectiveLevel`) for a player who might be an international/complex signee will silently misclassify them as MLB-level. `org-minors-query.ts` already had its own local `isInternational` check for a different purpose (grouping them into a separate roster box) — this is the same underlying data quirk, just a second place it bit.
20. **ETA reworked a second time the same day (2026-08-24) — Rees generalized his own fix.** The version in gotcha 19 (role-aware, real levels-to-climb, potential-driven pace) still had a real blind spot: it counted distance using the player's literal ROSTER level, so a player already performing at an MLB-caliber level for his role but stuck at (say) A+ behind a logjam still got a multi-year ETA purely because of where he was rostered. First fix: interpolate current Overall onto the role's own benchmark ladder to find a "Suggested Level" independent of roster level, and use *that* for distance instead — but this first pass still special-cased players literally on the active MLB roster (`level === 1`), auto-giving them ETA = this year regardless of whether their ability actually supported it. Rees caught that this was the same mismatch in the opposite direction and asked for it to be fully generalized: **Suggested Level (current Overall interpolated against the role's ladder) is now the only basis for ETA distance, for every player uniformly, with no roster-level shortcut anywhere** — not even for someone literally on the 26-man roster today. Confirmed against a real case: Paul Tremblett (SP, active MLB roster, Overall 56.7 vs. the SP bar of 65.4) now gets a real forward ETA (2033) instead of an automatic "now." `estimateSuggestedLevel()` (renamed from `interpolateEffectivePosition()`) in `scripts/compute-ratings.ts` is the current, real implementation — the Glossary page's ETA section was rewritten in step with each version so it never describes a formula that's no longer running.
21. **Overall/Potential now blend by real league handedness splits, not an even average of the vs-L/vs-R grades (2026-08-24, Rees's spec) — including K%avoid, added in a same-day follow-up fix.** Batting's Contact/Gap/Power/Eye/K%avoid and Pitching's Stuff/Movement/PBABIP/Control each come from a separate `_l`/`_r` scout grade (`player_ratings_snapshots` — confirmed zero null rate across all 13,986 players for every one of these fields, `ks_l`/`ks_r` included). Rather than average the two sides evenly, each is now weighted by how much of **real MLB playing time over the last 3 seasons** actually came against that handedness — hitters by real MLB **at-bats**, pitchers by real MLB **innings pitched** (per Rees: AB for the batting side, IP for the pitching side, both scoped to `level = 1` only). Real percentages, verified correct as of this refresh: batting 23.5% vs LHP / 76.5% vs RHP, pitching 33.2% vs LHB / 66.8% vs RHB — both close to real baseball's actual handedness population, cross-checked as a sanity signal since the split isn't documented anywhere. **`split_id` convention reverse-engineered, not documented by StatsPlus**: on both `player_batting_stats_snapshots` and `player_pitching_stats_snapshots`, `split_id=1`=overall, `split_id=2`=vs-LHP/vs-LHB, `split_id=3`=vs-RHP/vs-RHB (`split_id=21` exists too, low-volume and unidentified, ignored). Two explicit scope decisions from Rees: **Stamina has no `_l`/`_r` split field in this data at all** (confirmed by schema absence) so it stays unsplit — the ONLY Batting/Pitching component genuinely excluded, not Ks (see below); **Control does have a split and wasn't in Rees's original ask, but he confirmed include it anyway** since the data supports it. **Potential is deliberately excluded from this change** — StatsPlus exposes no `pot_*_l`/`pot_*_r` grades, so Batting Potential/Pitching Potential still use the single unsplit potential grades; **Platoon is also unaffected** — it exists specifically to measure the raw vs-L/vs-R gap itself, so it still reads `cntct_l`/`cntct_r` etc. directly rather than through the new blend. Implementation: `computeRatings()` in `lib/rating-engine.ts` gained a third parameter, `splits: HandednessSplits` (`{battingPctVsL, battingPctVsR, pitchingPctVsL, pitchingPctVsR}`); `scripts/compute-ratings.ts` computes the real percentages once per run (before the main per-player loop, alongside where `currentYear` is derived) and passes them through — only one call site in the whole codebase, confirmed via grep. Glossary page's "League Handedness Splits" subsection (under Overall & Potential, powered by `getHandednessSplits()` in `lib/queries.ts` — a read-only re-run of the exact same aggregation for display) shows the live percentages so the page never drifts out of sync with what's actually baked into `player_computed`.
22. **K%avoid was missed in gotcha 21's first pass, and hand-verifying the fix against real data surfaced a real, separate row-truncation bug in the split computation itself — both caught and fixed same-day (2026-08-24).** Rees spotted the miss by comparing the Glossary page against the code: `ks_l`/`ks_r` are real, fully-populated columns that got wrongly lumped in with Speed's genuine no-split-data case during gotcha 21's first pass. Fixed in `lib/rating-engine.ts` (added `ks_l`/`ks_r` to `RatingsInput`, added a `ksBlend` term to the Batting formula the same way as Contact/Gap/Power/Eye). **Separately, while hand-verifying the fix against a real player's stored `batting` value, found `sumBySplit()`'s per-500-player-chunk query in `scripts/compute-ratings.ts` had no `.range()` pagination** — a single unpaginated `.select()` silently caps at Supabase's default 1000 rows (gotcha 2), and a 500-player chunk over 3 years × 2 splits can produce up to 3,000 matching rows. Confirmed empirically with a throwaway debug script: two of five real chunks were landing on an exact, suspiciously round 1,000-row result, undercounting the true batting-split population by ~27% (2,164 of 2,956 real matching rows) and skewing the league split by ~0.1–0.2 percentage points — small, but enough that a from-scratch hand calculation against real player data wouldn't reproduce the stored value exactly, which is what surfaced it. Fixed by adding a `fetchAll()`-based inner pagination loop inside `sumBySplit()` so each player-id chunk's query pages through *all* its matching rows, not just the first 1000 — applied identically to both the real computation (`scripts/compute-ratings.ts`) and the read-only display copy (`getHandednessSplits()` in `lib/queries.ts`), which had been copy-pasted with the same bug. **A third, pre-existing gap in the same area was fixed opportunistically**: the `players` fetch in `compute-ratings.ts` (45,757 rows, ~46 pages) had no `.order("id")` — a direct instance of gotcha 13's general warning, here specifically skewing which player IDs ended up in `mlbPlayerIds` across pagination boundaries. All three fixes verified together against real data: the real MLB batting-AB split moved from a wrong 23.6%/76.4% to a correct 23.5%/76.5% (matching a clean, independent SQL join computed outside the script), and Ted Ketron's (CF) `batting` value now matches a from-scratch hand calculation of the fixed formula to full float precision (`55.74643191305599`, exact match both ways). **Lesson for any future `sumBySplit`-shaped code**: the chunk size (500) was chosen for the `.in("player_id", chunk)` filter list length, not because the resulting row count is safely under Supabase's 1,000-row default cap — those are two unrelated limits, and both need their own explicit handling.
23. **A catcher batting multiplier was added to counteract real, measured undervaluation of catchers (2026-08-24, Rees's spec).** Motivated by the Role Representation tables (gotcha/section added earlier the same day): C's Index was 36 by Overall and 71 by Prospect Potential — badly underrepresented, and notably NOT explainable the way SS's high index is (a narrow Role bucket alone would predict *over*-representation at the top, same logic as SS, so under-representation here is a real signal the formula underprices catchers, not bucket-narrowness noise). The existing fielding-side catcher bonus (`Catcher = (Block+Framing+Arm)/3 + 15`, see gotcha/Fielding section) barely moves Overall in practice — it's diluted 4x by `fielding_weight` (0.25), so +15 there is only really +3.75 Overall points. New `rating_weights.catcher_batting_multiplier` column (numeric, default 1.0 = no-op, active row set to 1.03) instead scales a catcher's own Batting AND Batting Potential directly, applied in `lib/rating-engine.ts`. **Gated on the computed Role bucket, not the raw StatsPlus `pos` field** — Rees's explicit spec: "only... catchers that are capable of playing at the MLB level." A new `isCatcherRole` boolean is computed early in `computeRatings()` (mirrors the Role priority-1 "C" branch's exact condition — `pos` isn't SP/RP/CL AND `pot_c` clears `ROLE_BUCKET_THRESHOLDS.c_pot`, i.e. 50 — and the Role branch itself was refactored to reuse this boolean instead of duplicating the threshold check, so the two can't drift apart). A player rostered at catcher (`pos = 'C'`) whose `pot_c` doesn't clear that bar falls through to another Role (usually DH) and gets none of this multiplier — confirmed on a real player (`pot_c=45`, Role="DH"): stored `batting` matched the unmultiplied formula exactly. A real Role="C" player's stored `batting` matched `BattingRaw × 1.03` exactly. Platoon is deliberately left untouched (same reasoning as gotcha 21 — it measures a raw vs-L/vs-R gap against absolute thresholds, and scaling both sides by the same multiplier would change platoon-worthiness thresholds in a way nobody asked for). **Measured real effect, worth watching**: C's Index moved to 108 by Overall (landed right around proportional — good) but to 154 by Prospect Potential (a bigger move than by Overall, because Prospect Potential blends both Potential and Overall, so the boost compounds across both terms) — flagged on the Glossary page itself as something to revisit if it keeps drifting further from 100 rather than settling. If Rees asks for a re-tune, the only thing that needs to change is the single `catcher_batting_multiplier` value on the active `rating_weights` row — no code change required, consistent with the whole point of keeping weights in a table instead of hardcoded.

24. **`getRoleLevelBenchmarks()` in `lib/queries.ts` was generalized to take a metric argument (2026-08-24, Rees's spec) — Batting and Fielding versions added alongside the original Overall one.** Same function, same aggregation, same MLB-active-roster-only / international-remap logic — now takes `"overall" | "batting" | "fielding"` and aliases whichever `player_computed` column to a common `value` field server-side (`` .select(`player_id,role,value:${metric}`) `` — PostgREST's `alias:column` select syntax) so the aggregation loop doesn't need to branch on metric. `RoleLevelBenchmarkCell.avgOverall` was renamed to `avgValue` to match (only one call site, the Glossary page, so this was a clean rename). Glossary page's "Role × Level Benchmarks" section now has three subsections (Overall/Batting/Fielding) sharing one `RoleLevelBenchmarkTable` component. Spot-checked Catcher/MLB's Batting average (48.0, n=59) against a direct SQL query — exact match. Confirms Fielding's existing +15 catcher bonus (gotcha/Fielding section) is real and large *before* `fielding_weight` dilutes it into Overall: catchers post a 73.0 MLB Fielding average vs. the next-highest role (SS) at 69.0 — the dilution by `fielding_weight` (0.25) down to Overall is what gotcha 23 addressed on the Batting side instead. SP/RP rows are intentionally left in the Batting/Fielding tables (not filtered out) — they reflect whatever incidental batting/fielding grades those pitchers carry, same as they were never excluded from the Overall table either.

25. **Fielding's flat catcher (+15) and infield (+5) bonuses were parameterized and test-removed, then reverted, same day (2026-08-24, Rees's spec).** Motivated by gotcha 24's new Batting/Fielding benchmark tables making the raw numbers visible: MLB Fielding averages were Catcher 73.0, SS 69.0, CF 61.8, INF 59.6, COF 55.3. Rees wanted to see what Overall looks like with the Catcher/Infield formula-level bonuses stripped out entirely, as a step toward a cleaner architecture where Fielding is a pure, un-convoluted defensive-skill number and ALL positional-scarcity premium lives on the Batting side via multipliers (like gotcha 23's catcher one) instead. First, parameterized the three bonuses (previously hardcoded literals `+15`/`+5`/`+0` in `lib/rating-engine.ts`'s Catcher/Infield/Outfield composite formulas) into new `rating_weights` columns `catcher_fielding_bonus`/`infield_fielding_bonus`/`outfield_fielding_bonus` (defaults 15/5/0, reproducing the original formula exactly) — confirmed the refactor was a true no-op by rerunning `compute-ratings` with defaults unchanged and diffing the MLB Fielding/Overall-by-role averages against a pre-refactor baseline: byte-for-byte identical. **Then ran the actual test** — set `catcher_fielding_bonus`/`infield_fielding_bonus` to 0, reran, captured results, reverted back to 15/5 immediately, reran again to confirm restoration (byte-identical to baseline a second time). **Key finding — do not zero these out without also re-tuning `catcher_batting_multiplier` substantially upward**: the 1.03 catcher multiplier from gotcha 23 was calibrated to replace only the *diluted* Overall contribution of the +15 fielding bonus (+15 × `fielding_weight` 0.25 ≈ +3.75 Overall points) — with the fielding bonus actually gone, catchers lost that ~3.75 points with nothing compensating for the other ~11.25, and Catcher's Role Representation Index (gotcha 22's diagnostic) got dramatically *worse* than it had ever been all session: 24 by Overall (worse than the original pre-session 36) and 12 by Prospect Potential (worse than the original 71). SS/CF/COF's indices moved up during the test, but this was confirmed to be mostly zero-sum redistribution (other roles' Overall dropping opened top-200 slots), not a direct reward — no SS/CF batting multiplier was added in this pass. Also confirmed: since SS and INF share one Infield composite (no separate SS formula exists) and CF/COF share one Outfield composite (no separate CF formula), the *existing* SS-vs-INF and CF-vs-COF Fielding gaps in the benchmark tables come entirely from real underlying grade differences in the player population, not from any formula-level positional bonus — only Catcher and Infield ever had an explicit bonus, Outfield never did. **Left in the reverted (production) state — 15/5/0, matching original behavior — pending Rees's decision** on whether to: (a) leave Fielding as-is, or (b) actually remove the bonuses for real this time paired with a much larger `catcher_batting_multiplier` and new SS/CF batting multipliers. Documented on the Glossary page's Fielding section (now shows live current values of all three, plus the test narrative) so this doesn't need rediscovering.
26. **Decision reached the same day (2026-08-24)**: rather than a full removal, Rees set `catcher_fielding_bonus = 12` (down from 15, a partial trim) and `infield_fielding_bonus = 0` (full removal) as the new production values — `catcher_batting_multiplier` left unchanged at 1.03. Recomputed and verified: 13,986 rows preserved. Real effect, better calibrated than either the original (15/5) or the fully-zeroed test (0/0): Catcher's Role Representation Index is now 108 by Overall / 131 by Prospect Potential — the Prospect Potential side in particular improved (was 154 at 15/5/1.03, overshooting; is now 131, closer to the 100 target) *without* touching the batting multiplier — the fielding-side trim alone pulled it back. INF's Index (which per gotcha 25 had no principled reason to carry a defensive bonus at all — it's an artifact of sharing the Infield composite with SS) dropped from 171/254 to a less-inflated 141/213, as intended. SS moved to 631/611 purely as a side effect of losing the same shared Infield bonus (SS and INF cannot be affected independently — there's no separate SS fielding composite) — not from any direct SS reward; **no SS or CF batting multiplier has been added**, and remains the natural next step if Rees wants to pursue rewarding those two premium spots the same way Catcher's was. Glossary page's Fielding section rewritten to describe this as the settled state (not a reverted test) — the "current values" line still reads live off `rating_weights` so it self-updates if these are tuned again.

27. **SS and CF batting multipliers added, catcher's raised 1.03→1.05, same day (2026-08-24, Rees's spec) — the natural next step flagged in gotcha 26.** New `rating_weights` columns `ss_batting_multiplier`/`cf_batting_multiplier` (both set to 1.025 on the active row; default 1.0/no-op), applied in `lib/rating-engine.ts` via the exact same mechanism as `catcher_batting_multiplier` (gotcha 23) — gated on the computed Role bucket, not raw StatsPlus position, mutually exclusive by construction. Implementation detail: `isCatcherRole`/`isSSRole`/`isCFRole` are all now computed early (before Batting) and reused by the Role classification further down (which previously duplicated the SS/CF threshold checks inline) — one source of truth for "which premium-position bucket does this player fall into," can't drift apart from Role itself. Verified against real players to full float precision in both directions: a real CF (Ted Ketron, previously verified batting under gotcha 21 as `55.74643191305599` unmultiplied) now shows `57.140092710882385` = raw × 1.025 exactly; a real SS (Andy Mroczkowski) matched raw × 1.025 exactly; a real catcher matched raw × 1.05 exactly. 13,986 rows preserved. **Real effect — SS/CF's Indices are now the most extreme in the whole Role Representation table**: C moved to 131/202 (Overall/Prospect Potential — up from 108/131, the 1.05 pushed it past proportional again, more so on Prospect Potential which compounds Potential+Overall per gotcha 23's original note), SS to 860/733 (up from 630/611), CF to 358/688 (up from 278/559). **Worth flagging explicitly**: SS and CF moved further in relative terms than Catcher despite a *smaller* multiplier (1.025 vs 1.05) — because they're much narrower buckets to start with (SS/CF are 0.9%/1.3% of the ranked population vs. Catcher's 4.2%), so the same proportional Batting lift produces a much bigger swing in how many climb into a fixed-size top-200. SS and CF were already reading as heavily overrepresented before any multiplier existed (this is the same "narrow bucket over-indexes by construction" dynamic the Role Representation section's own text warns about for SS) — this compounds on top of that dynamic rather than correcting a different one, unlike Catcher's original fix which was correcting genuine underrepresentation. Not treated as a bug — this was Rees's explicit, direct instruction with specific values, not a test — but flagged here and on the Glossary page (which now shows a live Role/Multiplier/Index table plus this full history) since it's a materially different situation from why the catcher multiplier was added in the first place, and worth knowing if these ever need to be revisited.

28. **Catcher Role classification gained a second, "marginal potential" qualifying path (2026-08-24, Rees's spec), surfaced by a real player Rees asked about directly.** He noticed Alex Nuno (MIL, `pos="C"` in StatsPlus) wasn't showing up as a catcher-role prospect anywhere and asked why — investigation found `pot_c=45`, five points under the single `pot_c ≥ 50` bar `ROLE_BUCKET_THRESHOLDS.c_pot` has used since the 2026-08-20 Role rework, so he was falling through the priority ladder to COF/DH despite being a real rostered catcher with respectable current defensive grades (`cblk=50`, `cfrm=50`). Rees's fix: **a player can now also qualify as Role="C" with `pot_c ≥ 45` (not just `≥ 50`) if his CURRENT blocking AND framing are both `≥ 50`** — catching readiness isn't purely a ceiling question; a 45-potential backstop who already blocks/frames like a 50 is a real catching prospect, not just a bat that happens to be listed there. New constants `c_pot_marginal: 45` and `c_marginal_block_frame: 50` added to `ROLE_BUCKET_THRESHOLDS` in `lib/rating-engine.ts` (kept as plain TS constants, not `rating_weights` columns — matches the existing precedent for `TBL_POS_THRESHOLDS`/the rest of `ROLE_BUCKET_THRESHOLDS`: these are classification cutoffs, not "rating quality" weights). `isCatcherRole` (already the single source of truth reused by Role classification, `isSSRole`/`isCFRole` exclusivity, and the catcher batting multiplier gate per gotcha 27) now checks `pot_c ≥ c_pot ∨ (pot_c ≥ c_pot_marginal ∧ cblk ≥ c_marginal_block_frame ∧ cfrm ≥ c_marginal_block_frame)` — all three downstream consumers picked up the new rule automatically with no other code changes. **Pre-verified against real data before implementing** (not just after): queried for `pot_c` in [45,50) with `cblk`/`cfrm` both ≥ 50 among non-pitchers, got 152 candidates (140 currently DH, 8 COF, 3 INF, 1 1B, zero SS/CF overlap) and spot-checked 10 of the DH ones by hand — all real rostered catchers (`pos="C"`, low `ifr`/`ofr` as expected for non-athletic backstops, varied real-looking `cblk`/`cfrm`/`carm` values, not a flat placeholder), confirming this wasn't a data-quality false positive before touching the formula. Recomputed and the real counts matched the pre-check exactly: Role="C" population 585→737 (+152), DH 2014→1874, COF 1487→1479, INF 1835→1832, 1B 516→515, no change to SS/CF. 13,986 rows preserved. Hand-verified Nuno's own new `batting`/`overall` values against the formula to full float precision — exact match both. **Real effect on Nuno specifically**: Role C, league-wide `prospect_rank` moved from #671 to **#311** — now inside the default league-wide top-500 view, which is what he was missing from in the first place. **Real effect on Catcher's Role Representation Index**: moved from 131→114 by Overall and 202→165 by Prospect Potential — both closer to proportional, because the newly-included players are mostly second-tier backstops rather than elite prospects, diluting the population more than they pad the top-200 count. Documented on the Glossary page's Role & Classification section (updated priority-table rule plus a full explanatory note) and here.

29. **`npm run build` (the actual production build) had never been run successfully all session — `tsc --noEmit` alone had been standing in for it and doesn't catch everything it does.** Found 2026-08-24 while prepping the first real Vercel deploy. Two independent, real blockers, both now fixed:
    - **`lib/rating-engine.ts`'s `countAtLeast()` used `grades.reduce((n, g) => ..., 0)` with no explicit type argument.** TypeScript's inference widened the accumulator to `number | null` (not just `number`) in this context, which cascaded into 6 more type errors everywhere `qp`/`qpp` (its return value) got used later in the file. `next build`'s type-check step is a hard build blocker (unlike bare `tsc --noEmit`, which reports the same issue but nothing actually gates on it) — fixed with an explicit `reduce<number>(...)`.
    - **`tsconfig.json`'s `include` array had `"scripts"` in it**, which pulled every standalone ingestion script (run only via `tsx`, never part of the deployed Next app) into the *app's* type-check scope. `scripts/investigate-draft-pool.ts` had pre-existing Supabase type errors that were quietly irrelevant to `tsx scripts/*.ts` execution (which doesn't consult `include`/`exclude` at all) but became a real build blocker the moment `next build` looked at the same `tsconfig.json`. Fixed by removing `"scripts"` from `include` — confirmed this doesn't break running any script via `tsx`, and confirmed a full `npm run build` succeeds cleanly with both fixes in place.
    - **Lesson**: if you're about to deploy for the first time (or after a long stretch of `tsc --noEmit`-only checking), run the real `npm run build` locally before trusting a Vercel deploy to catch these — it's a strictly stricter check.

30. **Contact/Control "floor gate" added, 2026-08-27 — Rees's spec, went through four real revisions the same day before settling.** Motivated by two real, concrete cases he spotted directly: David Vasquez (SP, Control 30) was ranking as the league's **#13 overall prospect** purely off elite Stuff/Movement, and Jimmy Gant (SP, Control 35) was **#30**. Root cause: Batting and Pitching are both pure linear weighted sums (`Contact×0.375 + Power×0.325 + Eye×0.15 + ...`, `Stuff×0.35 + Movement×0.4 + Control×0.3 + ...`) with no interaction term — nothing in the formula says "a tool this bad caps how much the others can compensate," so elite Power/Eye or Stuff/Movement can fully paper over a tool that would keep a real player off an MLB roster.
    - **Domain fact learned from Rees, not previously documented anywhere in this codebase**: the `cntct`/`pot_cntct` grade StatsPlus reports is **not an independent raw tool** — it's the game's own internal composite of Avoid-Ks and BABIP tendency. `mov`/`pot_mov` (Movement) is similarly a composite, of PBABIP and HRA. `ctrl`/`pot_ctrl` (Control), by contrast, is confirmed **standalone**. This is why the gate keys on Contact/Control alone rather than averaging Contact with the separate `ks` field — doing so would double-count the same underlying weakness once implicitly (inside Contact) and once again explicitly. See `MEMORY.md`'s `ootp-grade-composites` note for the standalone record of this fact.
    - **Revision 1 (single continuous ramp)**: below a threshold, multiply Batting/Pitching down, ramping linearly to a floor as the grade approaches 0. First real player check (Dean Salvucci, CF, Contact 40, a real proven performer) showed the initial threshold (45) was penalizing a grade that isn't actually unplayable — moved to 40 so an exactly-borderline real performer isn't touched.
    - **Revision 2 (two-tier step function)**: replaced the continuous ramp with an explicit two-step function — untouched above 40, a defined multiplier at 40 itself, a harsher one at 35-and-below. Verified against real players to full float precision at every step (a grade exactly at a boundary value resolves to the harsher tier, per Rees's spec: "35 and below" means 35 itself gets the low-tier multiplier, not the mid one).
    - **Revision 3 (age-gating)**: real verified results showed the two-tier gate was crushing young, clearly-still-developing real prospects (Gant age 22, Vasquez 23, Tyler Joyner 24) on their CURRENT Overall, while correctly penalizing Sadao Suzuki (age 33, a genuinely finished player with the same bad grades). Added `age` as a new `RatingsInput` field (previously not threaded into the rating engine at all — merged in from `players.age` in `scripts/compute-ratings.ts`, since it isn't on `player_ratings_snapshots`) and a `developed_age_threshold` weight (25, deliberately matching the prospect-pool cutoff below). **First pass fully exempted a developing player's current-side gate** (forced to a no-op 1) while still applying the potential-side gate unconditionally.
    - **The exemption in revision 3 had a real masking bug, found the same day by Rees**: Potential can never be graded below current-demonstrated ability (file header's harmonization decision #2, pre-existing and unrelated to this feature) — so for Vasquez specifically, once his current Pitching went back to its full ungated value (69.2), that became a floor UNDER his Potential, which happened to sit above his gated-Potential estimate (~55) — silently erasing the entire Potential-side penalty Rees explicitly wanted kept. **Revision 4 fixed this**: a developing player's current-side gate no longer gets a flat no-op exemption — instead it reuses the SAME gate value Potential gets (derived from the potential grade), so current and potential move together instead of colliding via the pre-existing floor rule. A developed player (age > threshold) is unaffected — his current-side gate still comes from his own current grade independently, same as before this feature existed.
    - **Final settled multiplier values** (also softened from Revision 2's original 0.90/0.75-for-both after reviewing real results): Contact (batters) — at 40: **0.90**, at/below 35: **0.80**. Control (pitchers) — at 40: **0.95**, at/below 35: **0.90**. Thresholds (40/35) unchanged throughout.
    - **New `rating_weights` columns** (9 total): `contact_gate_mid_threshold`/`contact_gate_mid_multiplier`/`contact_gate_low_threshold`/`contact_gate_low_multiplier`, the same four named `control_gate_*`, and `developed_age_threshold`. All have inline SQL `comment on column` documentation. An earlier single-threshold/floor design (4 differently-named columns) was dropped and replaced when Revision 2 landed — never shipped to a stable state, no migration-compat concern.
    - **Prospect-pool age cutoff added the same day, same reasoning**: `mlb_service_days < 45` (rookie eligibility) alone was letting in career minor-league journeymen who are technically still under the service-day cap but aren't real "prospects" in any meaningful sense. `scripts/compute-ratings.ts`'s `prospectPool` filter gained `age <= 25` (a new `PROSPECT_AGE_CUTOFF` constant, matching `developed_age_threshold`'s value deliberately — two different places the same conceptual line has to be enforced) as an additional requirement alongside the existing service-day and org/last-team-id checks. Pool size dropped from ~9,266 to **8,064**. A subtle disclaimer ("Eligible players: under 45 days of MLB service time and age 25 or younger.") was added to `FarmSystemReportBody.tsx`'s header — shared by both `/prospects` and `/TBL/prospects`, so a reader isn't left guessing why a rookie-eligible 27-year-old doesn't show up.
    - **Final verified real numbers** (refresh_run_id 12, all hand-checked to match the formula's math exactly): Gant (22) Overall 61.7 / Potential 70.0 / rank #116. Vasquez (23) Overall 62.2 / Potential 67.0 / rank #225 — current now genuinely below potential, no more floor-masking collision. Joyner (24) Overall 61.8 / Potential 66.0 / rank #295. Suzuki (33, developed, unaffected by any age logic) Overall 54.0 / Potential 53.7, still outside the prospect pool.
    - **`compute-ratings.ts` had a real, separate bug surfaced by this work — see gotcha 31.**
    - Role Representation (the `/glossary` weight-testing diagnostic) checked after every revision landed — SS/CF/Catcher's existing over/under-representation (a narrow-elite-defender-bucket dynamic Rees is already on record as comfortable with, unrelated to this feature) stayed in the same ballpark throughout; nothing new broke.

31. **`compute-ratings.ts` could never actually re-run against an already-computed refresh run — found 2026-08-27 while re-tuning weights for gotcha 30.** The script's final write step was a plain `.insert()`, no upsert, no pre-delete. That's fine the first time a refresh run gets its ratings computed (as part of the normal `refresh.ts` pipeline), but re-running `compute-ratings` against a run that's already been written once — exactly what every weight retune in this file's history has needed to do — collides on the `player_computed_refresh_run_id_player_id_key` unique constraint on every single row, every time. It's not clear how the many past same-day recomputes documented in gotchas 23-28 avoided this — most likely an undocumented manual `delete` before each one. **Fixed**: `.insert(batch)` → `.upsert(batch, { onConflict: "refresh_run_id,player_id" })`. No other behavior change; confirmed a full 14,036-row recompute succeeds cleanly against an already-computed run with the fix in place.

32. **Potential now has real (extrapolated) L/R split data of its own, 2026-08-28 — Rees's spec, closing a real anomaly he spotted directly.** He noticed Jeremy Porten showing Overall (83.3) *higher* than Potential (82.6) and asked why. Root cause traced exactly: Current Batting/Pitching have blended by real league handedness exposure since 2026-08-24 (gotcha 21), but Potential never got the same treatment — there's no `pot_*_l`/`pot_*_r` data from StatsPlus, so Potential kept reading the flat `pot_*` field directly. For any player whose current L/R split is asymmetric in the right direction, that alone can push blended Current above flat Potential, with nothing wrong in either number individually.
    - **Empirical finding, worth remembering**: the flat CURRENT grade is not a fixed function of its own L/R split. Real examples, same field: one player's flat Power exactly matches his R-side grade, another's matches his L-side grade, a third's sits at neither — confirmed across a real sample before designing anything, ruling out any single hardcoded convention like "flat = vs-RHP."
    - **Method** (`projectPotentialSplit()` in `lib/rating-engine.ts`): for each split-eligible grade, compute `t = (currentFlat − currentR) / (currentL − currentR)` — exactly, from real observed current data, not assumed — and the real differential `d = currentL − currentR`. Project `pot_R = pot_flat − t·d`, `pot_L = pot_flat + (1−t)·d`. Both of Rees's stated requirements hold **exactly by construction**, not approximately: `pot_L − pot_R` always equals the real current differential, and interpolating `pot_L`/`pot_R` by the same `t` always reproduces the known flat `pot_flat` value exactly. `t` is clamped to `[0,1]` as a guard against rounding noise pushing current flat slightly outside `[L,R]`; a null/near-zero-differential input falls back to `{l: flat, r: flat}`. **Explicitly flagged to Rees as a genuine estimate, not a recovery of ground truth** — every input here is itself a rounded proxy for a hidden continuous value (his own framing), so this carries the current relationship forward, it doesn't observe the true future one.
    - **Scope**: all 10 split-eligible tool grades — batting Contact/Power/Eye/Gap/Avoid-Ks, pitching Stuff/Movement/Control/HRA/PBABIP (Rees's explicit ask: "all metrics"). HRA is projected and stored like the other 9, but isn't a direct formula input anywhere (StatsPlus folds it into Movement's own composite already — gotcha 30) — so it has no blend to feed into, just its own inspectable projected split.
    - **New table: `player_projected_splits`** (RLS enabled, empty policy set, same default-deny posture as everything else) — one row per player per refresh, 20 columns (`pot_*_l`/`pot_*_r` for all 10 grades), unique on `(refresh_run_id, player_id)`, upserted (not inserted — same fix as gotcha 31) by `scripts/compute-ratings.ts` alongside `player_computed`. Deliberately its own sibling table, not folded into `player_computed` — matches the no-FK, join-by-`player_id`+`refresh_run_id` pattern in gotcha 1, and keeps this specifically inspectable/auditable on its own per Rees's request ("which we can then work with in the future").
    - **Potential's formula rewired**: `battingPRaw`/`pitchingPRaw` now blend the projected split (by the same real league handedness percentages Current already uses) instead of reading the flat `pot_*` field. Verified against real players to full float precision: Porten's Overall and Potential now land on the exact same value (83.26538429101541 both) since his potential grades equal his current grades in every field; two real prospects with big split asymmetries (Jaylin Melton, Chris Landry) projected correctly in opposite directions matching their own current L/R shapes.
    - **Second pass, same day**: the Contact/Control gate's potential-side check (`contactGateP`/`controlGateP`) still read the flat `pot_cntct`/`pot_ctrl` field even after the rewire above — reintroducing a smaller version of the same asymmetry for 73 real developed hitters whose blended CURRENT Contact sat a point or two above the gate threshold while flat POTENTIAL Contact sat exactly on it (e.g. Fernando Lopez: Overall 67.1 vs Potential 61.5 before this fix). Fixed by pointing both gate checks at the same blended value the rest of Potential's formula now uses. **Confirmed via direct query: zero players league-wide show Overall > Potential after this fix** (down from 73 after the first pass, effectively unbounded before any of today's work).
    - Role Representation checked and unchanged, as expected — it's driven by Overall's rank, which none of today's work touched (only Potential's own internal consistency).

33. **Control gate simplified to a single tier, 2026-08-28, same day — and a real boundary bug caught in the process.** Rees asked to remove the mid tier entirely for pitchers: no penalty at a blended Control of 40, a flat 0.90 for anything under it (dropping the separate, harsher 0.90-at-35-and-below tier gotcha 30 had set up — pitchers keep only one penalty level now, batters' Contact gate is unchanged). Implemented first as a pure data change (`control_gate_low_threshold` set equal to `control_gate_mid_threshold`, both at 40, both multipliers 0.90 — the existing two-tier `gate()` function degenerates into one tier automatically when its two thresholds are equal, no code change needed for that part).
    - **Real players moved the wrong direction on the first recompute** — Jimmy Gant (a case gotcha 30 specifically named) got WORSE, not better. Root cause: `gate()`'s boundary was a strict `grade > midThreshold`, so a grade sitting EXACTLY at 40 fell into the penalty branch, not the exemption — the opposite of what "remove the gate at 40" meant. This bit Gant specifically because his projected Potential Control has zero platoon split at all (both L and R project to exactly 40), landing him precisely on the boundary this bug got wrong.
    - **Fixed properly, not just patched around**: `gate()` gained an `atThresholdPenalized` parameter (default `true`, preserving Contact's original, already-verified, deliberately-inclusive behavior from gotcha 30 — "a real, proven Contact-40 performer still takes this light penalty, not a free pass"). Control's two call sites pass `false` explicitly, making Control's threshold exclusive (`grade >= 40` exempts, matching today's actual ask) while Contact stays inclusive (`grade > 40` exempts) — two genuinely different, both deliberate, boundary rules living side by side in one shared function rather than two near-duplicate ones.
    - **Final verified real numbers**: Gant — Overall 61.7 → **64.9** (fully ungated — both his current and potential gates land on the now-exempt 40 exactly), Potential → **73.4**, prospect rank #408 (the broken intermediate state) → **#33**, better than gotcha 30's original two-tier result (#116). **Vasquez did NOT improve from this specific change** — flagged directly to Rees rather than assumed: his actual Control grades (30 current, 35 potential) sit below 40 either way, so the single-tier simplification doesn't reach him; he was already getting the flat 0.90 before and after. Confirmed zero regressions: still zero players league-wide with Overall > Potential (same as gotcha 32's end state), and the full 14,036-row recompute matched exactly on both writes.

34. **A second foreign key on `player_computed` breaks every EXISTING bare `players(...)` embedded join, 2026-08-31.** Adding `comp_player_id` (a second FK to `players`, for the player-comp feature — see the "Computed" section above) made PostgREST unable to infer which relationship a bare `.select("...,players(...)")` embed means, anywhere in the codebase that already did this off `player_computed` — it started hard-erroring with `PGRST201` ("more than one relationship was found") on every request. This silently broke two things simultaneously: `/prospects` and `/TBL/prospects` started 500ing outright (`lib/queries.ts`'s `getOrgTeams()`, two call sites), and `scripts/compute-team-ratings.ts`'s automated GitHub Actions run started failing every cycle with no visible symptom on the site itself (`team_computed` just silently stopped advancing — caught by comparing its latest `refresh_run_id` against `refresh_runs`' latest, which had already moved 1 run ahead). **Fix, both places:** disambiguate with the explicit constraint name instead of the bare table-name shorthand — `players!player_computed_player_id_fkey(...)`, not `players(...)`. **Going forward:** any NEW embedded join off `player_computed` anywhere in this codebase needs this same explicit form from day one, or it risks the identical silent-until-you-check-the-data failure mode the team-ratings half of this bug was.

## 7a. Automated data refresh (2026-08-28)

The refresh pipeline is now fully automatic — nobody needs to open Claude Code, copy a StatsPlus cookie, or run `npm run refresh` by hand anymore, for a normal sim. Full design history and the options considered/rejected are in the conversation this shipped from; this section is the durable reference.

- **`.github/workflows/refresh.yml`** runs on its own GitHub Actions schedule (`*/30 * * * *`, every 30 min — GitHub doesn't guarantee exact timing, fine for this) plus `workflow_dispatch` for a manual/forced run. Its first step, `scripts/check-new-sim.ts`, compares StatsPlus's public, no-auth current-date endpoint against Supabase's latest succeeded `refresh_runs.game_date`; the rest of the job (`refresh` → `compute-ratings` → `compute-team-ratings`) only runs if that check found new data, or the trigger was manual.
- **No agent is in the trigger path at all.** This replaced an earlier design (a Claude routine polling Slack and firing the workflow via a GitHub PAT) once it became clear the check could run natively inside the workflow itself — fewer moving parts, no credential needed beyond the two secrets below, no dependency on Slack message wording.
- **Auth**: `STATSPLUS_API_TOKEN` (StatsPlus's own documented `?token=` mechanism, wiki.statsplus.net/web-tools/statsplus-api) replaced the old interactive session-cookie pair for `/ratings/` and `/gamehistory/` — verified directly against both before switching. Valid ~90 days from creation (current token created 2026-06-26 → **expires ~2026-09-24**; regenerate from the StatsPlus account Preferences page and update the GitHub secret when it does). Session-cookie auth is kept as a fallback in `lib/statsplus-client.ts` if the token is ever unset.
- **Admin Dashboard token-expiry reminder — still open.** He wants the token-expiry date above surfaced on `/admin` (§7b below) as a reminder, rather than a separate Slack/calendar ping. Not built yet — the expiry date isn't stored as queryable data anywhere (only in this doc and the GitHub secret's creation date, not readable back out) — needs a real place to read it from (a small settings table, most likely) before it can show up as a live value rather than a hardcoded one.
- **Secrets** (`SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `STATSPLUS_API_TOKEN`) live only in GitHub Actions' encrypted secrets store on `rmviersen/DSA` — never in a file, never visible via repo/folder access (relevant specifically because Cowork has folder access to this project — see §3's secrets-location note). Rees set these himself via `gh secret set`; no session ever saw the raw values.
- **The `league-refresh-status-report` routine** (formerly `league-refresh-cookie-watch`, repurposed rather than recreated) has no role in triggering anything anymore — Slack+Supabase read access only, no write/trigger capability, no GitHub credential of any kind. Hourly, it checks `refresh_runs` for a completed run it hasn't reported on yet, verifies it (row counts across `player_computed`/`player_projected_splits`/`team_computed` against `players`, and that `game_date` actually advanced), and DMs Rees one status line — a plain "looks good" or a specific concern. This is the answer to "have Cowork track outcomes," deliberately separated from the triggering mechanism so tracking never needed any privileged access.
- **Two real bugs found and fixed by the first live runs, 2026-08-28** (both now fixed and confirmed clean on a third run — `refresh_run_id` 14, all row counts exact): (1) the Actions runner was pinned to Node 20, which has no built-in global `WebSocket` — `@supabase/supabase-js`'s `RealtimeClient` requires one at client construction regardless of whether realtime features are used, so every run failed immediately in `check-new-sim.ts`. Fixed by bumping to Node 22. (2) `scripts/compute-team-ratings.ts` still used a plain `.insert()` for `team_computed`, the same class of bug already fixed for `player_computed` in gotcha 31 — just never ported to this script. A transient network hiccup lost the success response for an already-successful write, the retry collided with its own prior insert on `(refresh_run_id, team_id)`, and the whole run reported failure even though the underlying data was already fully correct (confirmed 32/32 rows via direct query before touching anything). Fixed the same way: `.insert()` → `.upsert(..., { onConflict: "refresh_run_id,team_id" })`.

## 7b. Admin Dashboard (`/admin`), 2026-08-28 — build in progress

Rees's intended home page as site owner — platform status, his own team, reporting, site performance. Cowork drafted the plan (four sections); Claude Code owns building all of it, since Cowork's role on this project is deliberately kept outside the codebase (see §2). Building in real, checkable pieces rather than all four sections at once — this section covers what's actually shipped so far.

**§1 Platform Status — done.** New page `app/admin/page.tsx` (automatically owner-only — nothing under `/admin` is in `middleware.ts`'s `GUEST_ALLOWED_PATHS`, no new auth work needed), new query layer `lib/admin-queries.ts` (kept separate from `queries.ts`, same reasoning as `org-minors-query.ts` — a self-contained addition, no reason to risk touching a file that might be mid-edit). Shows:
- Last refresh (game date, timestamp, status) — straight from the existing `refresh_runs`, no new table needed for this part despite the original plan assuming one.
- A freshness check — StatsPlus's real current game date (public, no-auth endpoint, safe for the deployed site to call directly) vs. the DB's latest succeeded `game_date`, using the exact same comparison `scripts/check-new-sim.ts` already does for the automated refresh, not a second implementation. **Needs `STATSPLUS_BASE_URL` set as a plain (non-secret) Vercel env var** — first time the deployed site itself calls out to StatsPlus rather than just a backend script; not yet confirmed set in the Vercel project, check before assuming this renders correctly in production.
- **New table `platform_events`** (RLS enabled, empty policy set, same default-deny posture as everything else) — a generic event/error log for things with no natural home elsewhere (a Supabase advisor finding, a manual note). Not tied 1:1 to `refresh_runs`, which already tracks its own success/fail independently.

**Two things the original plan got wrong, corrected before building:** `is_on_waivers`/`was_traded` are real fields but live on `players`, not `contracts` (only `no_trade` is on `contracts`). And `refresh_runs`'s actual column is `completed_at`, not `finished_at` as first written — caught by a live one-shot query against real data before it ever reached the page, not left for a future session to hit.

**Deliberately deferred out of §1**: live Vercel deployment status and live Supabase advisor findings in the feed — both need a new API token as a secret, which is worth its own explicit go-ahead rather than folding into this pass.

**Still to build**: §2 My Team (org roster, promotion-candidate flags off the existing Suggested-Level logic, roster-status flags off the corrected `players` fields), §4 Site Performance (Vercel Web Analytics + runtime errors — Vercel MCP access already confirmed working), §3 Reporting log (tracks Cowork-drafted reports that the **platform itself** sends to Slack via its own bot token — Cowork composes content, the platform delivers and logs it, deliberately not the reverse, since Cowork has no way to trigger anything from a dashboard button and no database access. Each report type gets its own trigger mode — `scheduled`, `manual`, or both — reusing the exact `schedule:` + `workflow_dispatch:` GitHub Actions pattern `refresh.yml` already proved out, not a new mechanism).

## 7. Running it locally

```bash
cd platform
npm install
cp .env.example .env   # fill in SUPABASE_SERVICE_ROLE_KEY
npm run dev
```

**As of 2026-08-27, the real `.env` doesn't live in `platform/` day-to-day** — it was moved to `C:\Users\rmvie\secrets\dsa-platform.env`, outside the `OOTP-Analysis` folder entirely, because that whole folder is being shared with Cowork (an automation agent) and `.env` holds real production secrets (`SUPABASE_SERVICE_ROLE_KEY`, StatsPlus session cookies, the owner-login secrets) — `.gitignore` keeps it out of git, but does nothing against a tool with plain folder/filesystem access. To run anything locally that needs real credentials (`npm run dev`, `refresh`/`compute-ratings`/etc.), copy it back in first: `cp ~/secrets/dsa-platform.env .env` (from `platform/`) — and remove it again afterward if the folder might still be exposed to something reading it directly off disk.
Opens on `localhost:3000`. The `refresh`/`compute-ratings`/`compute-team-ratings`/`import-draft-pool` npm scripts are backend-only — you shouldn't need to run them, but if data looks stale or missing, that's more likely "ask the backend session to run a refresh" than a front-end bug.

## 8. Coordination going forward

- Schema changes will show up in `lib/database.types.ts` (regenerate it, or ask, whenever something looks off) and get a line added to §4 above.
- If front-end work surfaces a data need that doesn't exist yet (a new computed field, a report needing data we haven't ingested), flag it back rather than trying to derive it client-side from raw tables — there's likely a reason it's not there yet (either not built, or a genuine open methodology question, like team financials).
- Both of us are committing to the same `main` branch on `github.com/rmviersen/DSA` — small, frequent commits with clear messages reduce collision risk given no branching strategy exists yet.
- **As of 2026-08-25, the `tbl-dsa` Vercel project is connected directly to this GitHub repo, production branch `main`.** Every push to `main` now auto-deploys to `dsa-reports.com` — no more manual `deploy_to_vercel` uploads needed for routine changes. (Tonight's initial deploy, before this connection existed, was a one-off manual upload — see §9's note on that mechanic; it doesn't apply going forward.) **Root Directory must be blank/`.`, not `platform`** — despite the local folder on Rees's machine being named `platform`, this repo's root *is* the app root (`app/`, `lib/`, `public/`, `scripts/` sit directly at the repo root on GitHub, confirmed via `git ls-tree -d origin/main`); there's no nested `platform/` subfolder inside the repo itself. Setting Root Directory to `platform` (with or without a trailing slash) fails the build immediately with "The specified Root Directory ... does not exist" — caught live on the first two pushes through the new connection before landing on the correct blank value.

## 9. Production deployment: owner/guest auth gate (2026-08-24)

**Owner nav's "Top Prospects" link now points at the guest view, 2026-08-27.** Rees noticed the full-site nav's "Top Prospects" still went to the old internal `/prospects` page (side-by-side tables) instead of matching what guests see. `SiteNav.tsx`'s `NAV_ITEMS`: `href: "/prospects"` -> `href: "/TBL/prospects"`, "for now" per his spec (implies this may get revisited, not a permanent decision to retire `/prospects`, which still exists at its own URL, just unlinked from here). Landing there also swaps the header itself -- `/TBL/prospects` renders `ReportHeader`, not `SiteNav` (see `ConditionalNav.tsx`'s pathname check) -- so a real owner clicking this link now sees the exact guest experience (cards only, no rankings table, the public two-item nav), with "Full Site →" to get back, same affordance the Preview-as-Guest toggle already provides. Verified live: link's href, resulting card count (200) and absence of `.prospect-table`, and the header's owner-specific "Full Site →" link all confirmed matching.

**Glossary Role Representation reverted to top 100, 2026-08-27.** `ROLE_REP_LIMIT` in `app/glossary/page.tsx` -- was widened 100 -> 200 on 2026-08-24, reverted back to 100 today ("top 100 players and prospects"). One constant shared by both tables (By Overall = players, By Prospect Potential = prospects), no other code touched -- `getRoleRepresentation()`'s default param was already 100, this just matches the page's explicit call back to it. Verified both section headings and body copy read "top 100" live, not just the constant's value in source.

**Background stitch texture, 2026-08-27.** Rees asked whether Claude Code can access background images to enhance the page -- answer given directly: no image-search tool is loaded this session, and even if one were, using a random web image as a permanent site asset would need a licensing check first, not something to just grab. Offered three options (bespoke CSS/SVG texture, a subtle DSA-logo watermark, or Rees supplying a real photo like the logo swaps); he picked the bespoke texture.

Built as a third `background` layer on `body`, `app/globals.css`: a hand-authored inline SVG data URI, a 64px tile with three small scattered X-shaped stitch marks (evokes baseball leather stitching as texture, not a literal ball graphic), at 6% opacity light / 8% dark (same "needs a touch more in dark mode" pattern as the gradient washes). New `--stitch-pattern` token, one value per theme, same structure as the existing `--bg-gradient` token -- `background: var(--stitch-pattern), var(--bg-gradient), var(--color-bg);` with `background-repeat: repeat, no-repeat, no-repeat;` (the pattern tiles, the two gradient washes don't). Verified via computed style, not just visual inspection (screenshots are unreliable this session, documented elsewhere in this file): confirmed the SVG data URI decoded and applied as the front layer in both themes, confirmed `background-color` still resolved correctly as the base layer despite the multi-layer shorthand (a real risk with this technique -- the browser's own computed-style serialization shows a trailing `none` for that slot, which is the expected/correct behavior, not a bug), and confirmed no horizontal overflow at 375px mobile width.

**Full data refresh (refresh_run_id 12), 2026-08-27.** Rees ran a sim and asked to refresh. Cookie validation passed cleanly (78,723 game-history rows). Several individual batch upserts logged a transient `TypeError: fetch failed` on attempt 1/3 (players, contracts, player_pitching_stats_snapshots, game_results, player_ratings_snapshots, player_computed) -- looked concerning in the raw log, but the script's own retry logic (3 attempts, backoff) recovered every one of them: confirmed by directly querying final row counts against what the script logged as expected (`player_computed`: 14,036/14,036; `team_computed`: 32/32; `player_ratings_snapshots`: 14,036/14,036; `players`: 45,807/45,807; `game_results`: 78,723/78,723) -- all exact matches, nothing missing. **Lesson: exit code 0 alone isn't proof of a clean refresh when the log shows mid-run failures -- cross-check real row counts against the numbers the script itself reported**, the same principle as every other "measure, don't assume" verification this session. New game date: 2031-09-29.

**Also confirmed**: `prospect_role_rank` (added 2026-08-27) populated automatically as part of this normal refresh -- no manual backfill needed this time, unlike when the column was first added (9,306/9,306 prospect-pool rows have it for run 12).

**Bug found and fixed while spot-checking the refresh**: the Top Prospects page subtitle still read "League-wide top 500 by Prospect Potential" in production, a leftover from before `TOP_PROSPECTS_LIMIT` was lowered to 200 earlier the same day -- the string was hardcoded separately in `FarmSystemReportBody.tsx` instead of reading the real constant. Fixed by exporting `TOP_PROSPECTS_LIMIT` from `lib/queries.ts` and interpolating it into the subtitle, so this can't silently drift out of sync again the next time the limit changes.

**Top Prospects list shortened 500 -> 200, 2026-08-27.** `TOP_PROSPECTS_LIMIT` in `lib/queries.ts`, one constant, shared by `/TBL/prospects` and the internal `/prospects`. Doesn't touch what the rating engine computes -- ranks beyond 200 are still real data, just not rendered/fetched here. Verified card count via `document.querySelectorAll(".prospect-card").length` on both pages, and that the last card's rank number actually reads #200 (not just a row count coincidence).

**Prospect cards: role tag + name sized up, 2026-08-27.** Small follow-up right after the Role Rank/layout-centering pass. `.prospect-role` 0.8125rem -> 0.9375rem (padding bumped slightly to match), `.prospect-name` 1.1875rem -> 1.3125rem. Verified no overflow at either desktop content width or 375px mobile.

**New computed field + prospect-card layout restructure, 2026-08-27.** Rees asked for four things together: Stats to start at the same x as the meta line (not flush under Role/Name), Role+Name and Rank vertically centered, a real fix for "Org Rank" itself showing double-wide spacing, and a brand-new "Role Rank" callout -- leaguewide rank within role bucket (SP/RP/C/1B/INF/SS/COF/CF/DH), by prospect potential.

**New computed column: `player_computed.prospect_role_rank`.** Mirrors the existing `prospect_org_rank` pattern exactly (same file, `scripts/compute-ratings.ts`) -- same prospect-pool filter, same "sort descending by `prospect_potential`, rank within groups" logic, just grouped by `role` instead of `organization_id`. Distinct from two pre-existing, easy-to-confuse columns that do NOT already cover this: `role_org_rank` (role rank scoped to ORG, by Overall) and `pos_rank` (leaguewide by Overall, not Potential, not prospect-pool-scoped). Added via `apply_migration` (plain nullable `integer`, no backfill-in-migration), then backfilled for the current latest `refresh_run_id` in a separate one-off SQL statement (a `RANK() OVER (PARTITION BY role ORDER BY prospect_potential DESC)` window function, scoped to `prospect_rank IS NOT NULL` rows -- exactly StatsPlus's own prospect-pool definition) so it's populated immediately rather than waiting for the next full refresh. Verified the backfill matched expected counts per role before moving on (`with_rank` == `prospect_pool_count` for every role bucket). `database.types.ts` regenerated via the Supabase MCP tool -- **gotcha hit doing this**: the tool's raw output is a JSON envelope (`{"types": "...with \n escapes..."}`), not the TypeScript file content directly; a naive `cp` of the saved tool-result file produces a syntactically-broken one-line "TS" file. Had to parse the JSON and write out the `types` field's actual string content (via `node -e`, no `python3` available in this environment) to get back proper multi-line, prettier-formatted output -- confirmed by `git diff --stat` showing a minimal, sane diff (4 insertions) instead of the whole 3194-line file replaced.

**Historical baseline comparisons against pre-2026-08-27 refresh runs will show no Role Rank delta** (not a bug) -- older `player_computed` rows never had this column computed, so `prospectRoleRank` in `ProspectDelta` correctly resolves to `null` (same null-safety pattern as every other delta field) rather than erroring or showing a misleading number.

**Card layout restructure** (`app/globals.css` + `ProspectTable.tsx`) -- Role+Name split out of the old single `.prospect-headline` row into its own `.prospect-namerow`, sitting beside a new `.prospect-details` column (meta line + stat line stacked, sharing one left edge). This is what makes "stats start at the same x as the bio info" true *structurally*, for any name length, rather than needing a hand-tuned indent per card: `.prospect-card-body` is a flex row with `align-items: center`, vertically centering the (shorter) namerow against the (taller, two-line) details column -- also what "Role and Name centered vertically" means concretely. `.prospect-card-rank` got `align-self: center` for the same reason, independent of the namerow/details centering. Each meta callout (Age, Level, ETA, Org Rank, Role Rank, Draft) is now its own flex child (`.prospect-meta-item`) with a real `gap` between them, replacing the old single-text-blob-plus-`word-spacing` approach -- **that was the actual "Org Rank" bug**: `word-spacing` stretches every space character in the whole string, including the one INSIDE the two-word label "Org Rank" itself, not just the gaps between callouts. Structural per-callout children fix this permanently, not just for this one label.

**Real regression caught and fixed before shipping**: the body-restructure broke mobile layout — `.prospect-card-body`'s new flex ROW (namerow beside details) doesn't wrap by default, so at 375px width `.prospect-namerow`'s fixed content width squeezed `.prospect-details` down to ~25px, wrapping every single meta callout onto its own line and inflating one card to 678px tall. Fixed with a `.prospect-card-body { flex-direction: column }` override in the existing `@media (max-width: 640px)` block -- stacks name/role above meta/stats at narrow widths instead of squeezing them side by side. Card height back to a sane ~224px after the fix. Caught by actually checking `.getBoundingClientRect().height` at the mobile viewport preset, not assumed from the desktop layout looking right.

**Prospect cards, round 3: spacing + container width follow-up, 2026-08-26.** Same day, right after the gradient/hover/role-color pass shipped. Two small asks:

- **More offset between the name and the meta line specifically**: `.prospect-meta` gained `margin-left: 1rem` on top of `.prospect-headline`'s own flex `gap` (0.875rem) -- deliberately an addition on `.prospect-meta` alone, not a bump to the shared `gap`, so role-pill-to-name spacing stays as it was and only name-to-meta widens (14px -> 30px measured). `word-spacing` on the meta text also bumped again, 0.15em -> 0.2em.
- **Narrower page container**: `--content-max-width` 1400px -> 1200px. Shared by `.site-nav` and `.site-main`, so this narrows the header row and every page's content sitewide (not just Top Prospects) -- the two were always meant to line up, so narrowing one without the other would have looked broken.

**Broader visual design pass, 2026-08-26.** Rees's ask after the logo fix: "a bit more dynamic aesthetic vibes," a hover "movement effect" on cards, more horizontal breathing room in the card meta line, a de-emphasized stat line, a distinct name color, and 5-family/9-role color-coded role pills. All in `app/globals.css` unless noted.

- **Background**: a new `--bg-gradient` token (two soft radial washes, navy top-left + brass bottom-right, tuned per theme) layered under `--color-bg` on `body`, with `background-attachment: fixed`. That fixed attachment is deliberately what supplies "motion when scrolling" -- the gradient stays pinned in the viewport while content scrolls past it, a real depth/parallax effect achieved with zero JS and no dependency on this session's frozen animation-compositor clock (a CSS `@keyframes`/`transition`-driven effect would have that dependency; native scroll compositing does not). Considered and deliberately skipped: a scroll-triggered per-card reveal animation -- that WOULD hit the frozen-clock problem again, unverifiable this session, so left as a future ask rather than shipped un-verified.
- **Card hover "lift"**: `transform: scale(1.02)` + `box-shadow: var(--shadow-md)` (up from resting `--shadow-sm`) + `z-index: 1` so the enlarged card renders over its neighbors instead of just visually overlapping them. **Hit the frozen-animation-clock issue again while verifying** -- real `:hover` engaged via `computer{action:"hover"}` (confirmed with `element.matches(':hover')`), `z-index` (no `transition` on it) applied correctly, but `transform`/`box-shadow` (both have a `transition`) read back as their pre-hover resting values until the transition was forced off (`transition: none !important`), at which point they showed the correct target values (`scale(1.02)`, `shadow-md`). Same exact diagnostic technique as Step 7's dark-mode row-stripe check -- worth remembering as the default first move whenever a post-interaction computed style looks wrong in this session specifically.
- **Meta line spacing**: `.prospect-headline`'s flex `gap` widened (0.625rem → 0.875rem) and `.prospect-meta` got `word-spacing: 0.15em` -- widens the room around the "·"-separated Age/Level/ETA/Org Rank/Draft segments without restructuring them into separate elements (kept the existing "·" convention rather than switching to a different visual language, since the ask was "a bit more," not a redesign).
- **Stat line**: de-bolded and re-colored to `var(--color-text-muted)` -- both the CSS (`.prospect-stats`) and the JSX (`ProspectTable.tsx` no longer wraps `statLine(r)` in `<b>`, not just an override) were changed, so there's no leftover bold-by-default markup underneath.
- **Player name color**: `var(--color-border-strong)` (brass), not a new token -- this one already has correct light/dark values for use as TEXT (light `#a8763a`, dark `#d9a75c`), the same distinction the `--color-heading` fix caught earlier this session (a color safe as a *background* isn't automatically safe as *text* once the page goes dark). Reusing it here means the name color is provably theme-safe by construction, not something that needs its own contrast check.
- **Role pill color-coding**: 5 position families (pitchers, catchers, infield, outfield, DH), 9 roles total, each a `--role-*` custom property (one value per role, both themes) rather than 9×2 separately hand-picked tint/border/text combinations. `color-mix(in srgb, var(--role-color) N%, ...)` computes the pill's tinted background and border from that single hue at render time; a plain solid fallback is declared on the same CSS property immediately before the `color-mix()` version (standard progressive-enhancement pattern -- if a browser doesn't understand `color-mix()`, the whole declaration is invalid and skipped, leaving the earlier plain-color fallback intact instead of an unstyled pill). Reused existing tokens where a family already matched an existing accent (SS -> `--color-green`/`--color-green-bright`, DH -> `--color-tan`/`--color-border-strong`) instead of inventing unrelated hues for those two. `ProspectTable.tsx` gained a small `ROLE_CLASS` lookup mapping each of the 9 `ProspectRow.role` strings to its CSS class; any role outside that list falls back to the pill's original neutral styling rather than guessing a bucket. Verified all 9 classes actually appear across real card data and resolve to 9 genuinely distinct computed colors in both themes (not just declared distinct in source).

**Real bug found and fixed: team logos were stale, not broken, 2026-08-26.** Rees noticed "many of them look outdated or not matching statsplus logos" -- a real bug, not a subjective ask. Root cause: `lib/display-helpers.ts`'s `teamLogoUrl()` guesses a filename slug (`{name}_{nickname}`, lowercased/underscored) and was never fully verified against real StatsPlus URLs (its own comment already admitted "only spot-checked a handful"). The real, current filenames on StatsPlus's own team pages carry a numeric suffix our guess didn't include -- confirmed by comparing our unsuffixed URL against the actual `<img src>` on every real team page (checked via Rees's logged-in Chrome session using the `claude-in-chrome` connector, since team pages are auth-gated and this session's own sandboxed browser can't log in). **Every one of the 32 active teams uses the same `_110` suffix** -- likely a pre-scaled size variant (the loaded image's own `naturalWidth` is exactly 110px), not a per-team id. Confirmed the suffixed URL is publicly reachable with no login required (fetched from this session's own unauthenticated browser, `credentials: "omit"`, got a clean 200 + `image/png` for all 32 teams) before using it site-wide.

The bug was silent specifically because **both the old and new files return 200** -- the unsuffixed original logo file is still sitting on StatsPlus's server for teams whose logo was later updated, so our site was never showing a broken-image icon, just a stale-but-valid one from before that team's last StatsPlus-side logo change. That's why it read as "outdated," not "broken."

Fix: `teamLogoUrl()` now appends a `LOGO_SIZE = "110"` constant to the slug. One shared helper, so this fixes both consumers at once -- `ProspectTable.tsx`'s cards and `queries.ts`'s `getTeamRankings()` (System Rankings' logos). If StatsPlus does another logo regen in the future and this recurs, the fix is the same process: open a real team page (needs an authenticated session -- ask Rees, or use `claude-in-chrome` if his Chrome profile is still logged in), read the actual `<img src>`, compare its suffix against `LOGO_SIZE`.

**Prospect cards, round 2: layout/density/color feedback, 2026-08-26.** Same session, right after the first card version shipped -- Rees asked for three more things:

1. **Meta line moved beside the name, not below it.** Age/Level/ETA/Org Rank/Draft used to be their own line under Role+Name; now they're inline on the same row (`.prospect-headline` in `app/globals.css`, `ProspectTable.tsx`), with the stat line staying on its own row below. This is what actually shortens each card -- combined with tighter card padding (`0.75rem 1rem` -> `0.625rem 1.125rem`) and a smaller stats-line top margin, per Rees's "tighten up the vertical spacing" ask.
2. **Cards lightened in dark mode.** New `--color-card-bg` token, separate from `--color-surface` (which the filter bar and other surfaces still use unchanged) -- equal to `--color-surface` in light mode (already the lightest a card can be against a cream page, so no visible light-mode change), but `#1c2f40` in dark mode, a step lighter than `--color-surface`'s `#101f2b` so cards visibly lift off the page instead of nearly blending into it. Confirmed via computed style: card bg now measurably different from the filter bar's bg in dark mode, same in light mode.
3. **Fonts bumped up** across the card: name `1rem` -> `1.1875rem`, meta/stats `0.8125rem` -> `0.9375rem`, role pill `0.6875rem` -> `0.8125rem`, rank number `1.375rem` -> `1.5rem`, logo `34px` -> `38px` (proportional bump, not requested directly but needed to not look undersized next to the bigger name).

**Same message, a second ask arrived mid-implementation**: move the org abbreviation to sit next to the logo (it was at the far right of the name row, disconnected from the logo it identifies) and color-match it to the team's real color. Org now sits as a caption directly under the logo circle (`.prospect-team`, a small flex column wrapping both) instead of floating at the end of the meta line. **Team-color matching is NOT done** -- checked first: `teams` in `database.types.ts` has only `id`/`name`/`nickname`/`parent_team_id`/`updated_at`, no color column, and nothing in `statsplus-api-inventory.md` suggests StatsPlus exposes one either. Org text is plain muted text for now. Real next step needs a decision from Rees: hand-maintain a ~32-team color lookup table (accurate, but real data-entry work with no existing source to pull from), or attempt automated dominant-color extraction from the logo images (technically possible but unreliable for badge-style logos with lots of white/black/neutral pixels, and would need to run somewhere -- doing it live per-card for 500 rendered cards is not viable, would need to precompute and cache per team). Flagged, not guessed at.

Verified same way as the first round: fresh-tab console clean, computed styles sampled directly (card bg, font sizes, org position relative to logo) rather than assumed from the code, H/P filter and BIO toggle still work post-refactor, no overflow at 375px mobile width. Rees said after this round he wants to "revisit the visual design of the page" more broadly -- not yet scoped, watch for that as the next real ask rather than assuming it's more card tweaks.

**Top Prospects: table rows replaced with cards, 2026-08-26.** Rees said the double-row table looked "clunky" once the site had real colors and dark mode, and asked for short, wide cards instead, reusing the same rounded-surface-plus-shadow "bubble" look as the `.option`/`.diag-item` boxes from the original approved visual-refresh proposal. **Mocked up first and approved before touching real code** -- built a standalone HTML mockup using the actual site tokens (not Claude's own design-system tool, which would have used the wrong palette entirely), published as an artifact, Rees confirmed the layout and scope (prospects only, not System Rankings) before implementation started.

- `app/_components/ProspectTable.tsx` -- the `<table>`/two-`<tr>`-per-player structure (rowSpan tricks for Rank/Logo/Org spanning both rows) is gone, replaced with one `<div className="prospect-card">` per prospect. Same fields, same order, same filter/search/bio-toggle/delta-badge logic -- only the DOM shape and CSS changed, not any data or interaction logic. The old separate main-row/detail-row columns (Age, Level, ETA, Org Rank, Draft Yr/Rd/Pick) are now one flowing "meta" line under the name (`Age 23 · AAA · ETA 2032 · Org Rank #1 · 2026 R1 Pk4`), matching the "value LABEL" convention already used elsewhere (the stat line, TeamRankingsTable's top-3 list) rather than bare unlabeled table cells.
- `app/globals.css` -- new `.prospect-cards`/`.prospect-card`/`.prospect-card-rank`/`.prospect-logo`/`.prospect-card-body`/`.prospect-headline`/`.prospect-role`/`.prospect-name`/`.prospect-org`/`.prospect-meta`/`.prospect-stats` rules. Card styling is literally `background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-md); box-shadow: var(--shadow-sm)` -- the exact same token combination as the approved proposal's `.option`/`.diag-item` cards, so it's provably the same visual language, not a new one invented for this. The old `.prospect-table` CSS rule is untouched and still in use -- **`TeamRankingsTable.tsx` (System Rankings) still uses it**, deliberately not converted (Rees's explicit scope: "just prospects for now").
- Reuses the existing `dsa-fade-in` keyframe (Step 6) on the card list wrapper when the H/P or Role filter changes, same as the old `.table-wrap` did -- no new animation needed.
- Logo now renders inside a 34px circular frame (`.prospect-logo`, `border-radius: 50%`) instead of a bare `<img>` in a table cell; falls back to the org abbreviation's first 3 letters as text when a team has no logo URL, instead of rendering nothing.
- **Found and fixed a pre-existing hydration-mismatch console warning while verifying this** (unrelated to the card change itself -- present on any page, existed since Step 7 shipped dark mode): the inline `<head>` script that applies `.dark` before paint means the server-rendered `<html className>` and the client's post-script `<html className>` legitimately differ on first render, which is the standard, harmless "flash of wrong theme" tradeoff every dark-mode-via-localStorage site makes -- but React logs a hydration warning about it by default. Fixed with `suppressHydrationWarning` on `<html>` in `app/layout.tsx`, the standard fix for exactly this pattern (same one `next-themes` recommends). Confirmed the warning is gone in a fresh tab (an already-open tab kept showing it -- the usual stale-tab-after-server-restart gotcha, not a real regression).
- Verified: 500 cards render on `/TBL/prospects` and `/prospects` with real data (real team logos, real bios); H/P and Role filters correctly narrow the card set; name search and empty-state message both still work; BIO toggle expands/collapses correctly; dark mode confirmed on cards specifically (card surface is a visibly lighter fill than the page background in both themes, not just a copy of the page bg -- the actual "bubble" effect Rees asked for); fits without overflow at 375px mobile width (org abbreviation wraps to its own line below the name at that width); System Rankings alongside on `/prospects` is unaffected (still 64 table rows, untouched CSS).

**Dark mode follow-up: unreadable headings fixed, 2026-08-26.** Rees reported "a lot of dark text I can't read" right after Step 7 shipped. Cause: several page headings (`.page-header h1` in `app/globals.css`, plus `glossary/page.tsx`'s section titles, `FarmSystemReportBody.tsx`'s report title, and `login/page.tsx`'s heading) used `var(--color-navy)` as *text* color. Navy works fine as a *background* on a dark page (that's the header band, the table header row -- deliberately left unchanged in Step 7's dark palette), but as foreground text on that same near-black page background it's a completely different problem: navy (`#0b3049`) and the new dark bg (`#0a1620`) are close enough in darkness that the heading nearly disappeared -- a real contrast miss, not the frozen-animation-clock environment quirk from Step 6/7's other findings. Fixed by adding a new `--color-heading` token, separate from `--color-navy`: equal to navy in light mode (no visible change there, confirmed), but a lightened blue (`#7fabc9`, reusing Step 2's already-established dark-mode `--primary` hex rather than inventing a new color) in dark mode. Every text-color use of navy was swapped to this new token; every background use of navy was deliberately left alone. **Lesson for any future theme work**: a brand color chosen to work as a background doesn't automatically work as text on the same surface once the surrounding page gets dark -- background-safe and foreground-safe are different questions and need checking separately, not assumed from one successful case.

**Step 7 of the visual refresh: dark mode toggle, 2026-08-26.** Rees wants to eventually make dark the site's one permanent look (no toggle at all), but explicitly asked to test and compare against light first before committing — so this ships as a purely additive, client-side toggle, not a default change. Nothing changes for a visitor who never clicks it.

- **`app/_components/ThemeToggle.tsx`** (new) — a 🌙/☀️ pill button that flips a `.dark` class on `<html>` and persists the choice to `localStorage` (`dsa-theme`), per-browser. Deliberately *not* a cookie like the owner/preview-guest system — light/dark has no security dimension, every visitor (owner or guest) picks their own independent of anyone else, so there's no reason for `middleware.ts` to see it.
- **No-flash-on-load**: a small inline `<script>` in `app/layout.tsx`'s `<head>` (plain `dangerouslySetInnerHTML`, not `next/script` — needs to run synchronously during HTML parsing, before paint) reads `localStorage` and applies `.dark` before the page renders, so a dark-mode visitor never sees a flash of light theme on load or navigation. Confirmed working: set `localStorage` then hard-navigate to a fresh page — `.dark` is present immediately, before any client JS could plausibly have run it post-hydration.
- **The `.dark` class itself wasn't new** — Step 2 (2026-08-25) had already added an inert `.dark` block for shadcn's separate `--background`/`--primary`/etc. token system, unused by any real component. This step extends the *same* selector with real overrides for the actual `--color-*` tokens all 128+ call sites render with (`app/globals.css`), reusing Step 2's hex choices (lightened navy/brass so they still read on a dark ground) rather than re-deriving a new palette. Deliberately left unchanged: `--color-navy`/`-tan`/`-brown`/`-green` and their variants — the brand accent colors (header band, table header, brass underline) read fine as-is against a near-black background in both themes; only the page's own bg/surface/border/text/table-tint tokens got dark equivalents.
- Two real hardcoded-color bugs fixed along the way (both would've shown a stray white box in dark mode): `.filter-bar select`'s `background: white` → `var(--color-surface)`, and the login page's password `<input>` (no background/color set at all before, relying on browser default) → explicit `var(--color-surface)`/`var(--color-text)`.
- Toggle placed in both `SiteNav.tsx` (internal pages) and `ReportHeader.tsx` (public `/TBL/prospects` pages) — grouped with the other right-side header items (Preview as Guest / login link / game-date badge) into a new `.site-nav-actions` flex wrapper, so `.site-nav`'s `justify-content: space-between` row doesn't reflow unpredictably depending on which conditional items are present.
- **Hit the same frozen-animation-clock environment limitation as Step 6, and it's worth the general lesson**: right after toggling, the table row stripe/hover colors (`tbody tr:nth-child(even)`, which has `transition: background-color 0.12s ease` from Step 5) read back as the *old* light color via `getComputedStyle`, even after forcing the CSS variable directly and even with an inline `!important` override — looked exactly like a real bug. Root-caused by disabling the transition (`row.style.setProperty('transition','none','important')`) and re-checking: the color was correct all along (`#142735`, exact target value). Same cause as Step 6's Motion "bug": this session's Browser pane never advances `document.timeline`, so any property with an active CSS transition gets stuck displaying its *starting* value forever in this environment, independent of whether the underlying CSS is right. **General technique for next time**: if a computed style looks wrong immediately after a state change, check whether that property has a `transition`/`animation` before concluding it's a real bug — temporarily forcing `transition: none !important` on the element isolates environment-clock issues from actual CSS bugs in one step.
- Verified working end-to-end: toggle flips instantly on `/players` (SiteNav) and `/TBL/prospects` (ReportHeader) with no page reload; persists correctly across navigation; reverses cleanly back to light; renders correctly on `/login` (which unexpectedly also gets the full `SiteNav`, pre-existing behavior, not something this step touched); fits without overflow at mobile width (375px). Not yet deployed to production or shown to Rees — local-only pending his review.

Rees asked to get `/prospects` and `/report` (Top Prospects + System Rankings) live in production tonight while every other page (`/players`, `/draft`, `/org-minors`, `/glossary`) stays hidden from the public — with himself auto-logged-in to see everything, and anyone else he shares the link with treated as an unauthenticated "guest" seeing only the approved pages, no login step for guests at all.

**Architecture — deliberately NOT full Supabase Auth.** With exactly one privileged user (Rees) and an undifferentiated, no-login guest tier, real accounts/sessions would be meaningfully more to build and maintain for a capability nobody asked for (telling individual guests apart). Instead: a single shared owner password, an HMAC-signed cookie, and Next.js Middleware gating every route. This is a **page-visibility gate only, not a data-security boundary** — the real protection is the Supabase RLS default-deny setup from §3, completely independent of this system and unaffected by it.

- `lib/owner-cookie.ts` — `expectedOwnerCookieValue(secret)` HMAC-SHA256-signs a fixed payload using Web Crypto (`crypto.subtle`, not Node's `crypto` module — needed because Middleware runs on the Edge runtime, which doesn't have Node's `crypto` or a reliable `Buffer`). The signature itself (not the password) is what's stored in the cookie.
- `middleware.ts` — runs on every request except static assets. If a valid signed `dsa_owner` cookie is present, the request passes through untouched (owner: every route). Otherwise, only `GUEST_ALLOWED_PATHS` (`/TBL/prospects`, `/login`) are allowed through; everything else redirects to `/TBL/prospects`. **To open up a new page to guests, add its path to `GUEST_ALLOWED_PATHS` — no other code changes needed** (or nest it under an already-allowed path, like `/TBL/prospects/farms` did — the prefix match already covers it).
- `/login` (`app/login/page.tsx`) + `/api/login` (`app/api/login/route.ts`) — a plain password form. Correct password → sets the signed cookie with a 1-year expiry (Rees's "automatically logged in thereafter" spec) and redirects home; wrong password → back to `/login?error=1`.
- **"Preview as Guest" toggle, added 2026-08-25.** Lets an already-logged-in owner see exactly what a guest sees without logging out. Second cookie, `dsa_preview_guest` (name in `lib/owner-cookie.ts`), deliberately *unsigned* — it can only ever narrow an already-verified owner's access, never grant access to anyone who doesn't already have a valid `dsa_owner` cookie, so it doesn't need HMAC protection. `middleware.ts` only takes the full-access branch when the owner cookie is valid **and** this one is absent; toggled via plain `<a href>` GET requests (not `next/link`'s `<Link>`, which prefetches links in view by default — a prefetched state-changing GET would silently flip the toggle before anyone clicked it) to `/api/preview-guest?action=enter` / `?action=exit`, which redirect back to wherever the click came from (`Referer` header, with a sane fallback). That route is a no-op for anyone without a real owner cookie — verified live: clearing the owner cookie and hitting the URL directly does nothing. Display logic needs `isRealOwner` and `isPreviewingGuest` as two separate booleans, not one combined `isOwner` — a genuine guest and an owner-previewing-as-guest see the same restricted pages, but the header still needs to tell them apart (guest gets "Owner Login"; previewing owner gets "Exit Guest Preview" instead, since they don't need to log in again). `SiteNav.tsx` (owner pages) shows "Preview as Guest"; `ReportHeader.tsx` (`/TBL/prospects` pages) shows the three-way state on the right side of the nav.
- Cookie comparison is a plain `===` string check, not constant-time — an accepted low-stakes tradeoff (this gates page-visibility *timing*, not real data).
- Required env vars (Vercel dashboard only — no MCP tool exists to set these, and Claude Code must never enter secrets into a field itself): `OWNER_PASSWORD` (what Rees types at `/login`) and `OWNER_COOKIE_SECRET` (internal HMAC key, opaque random string). Both generated once and handed to Rees to paste in himself. `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY` are also required at **build time**, not just runtime — confirmed 2026-08-24: `lib/queries.ts` and `lib/org-minors-query.ts` both create their Supabase client at module scope, which Next.js's "Collecting page data" build step touches even for `force-dynamic` pages (it still has to statically analyze the root layout). No env vars → the build itself fails, not just the deployed app.

**Three real N-scan performance bugs found and fixed in `lib/queries.ts`, 2026-08-25.** Rees flagged `/TBL/prospects` as slow to load (both for him live and visibly in this session's own dev-server request logs: 20-23 seconds per load). All three bugs shared the same shape: fetch an entire large table unfiltered, in JS-paginated 1000-row chunks, just to derive a small result — instead of pushing the real filter/sort/limit down to the database query itself.

1. **`fetchComputedPlayers()`** (powers `/players`, `/draft`, and — via `getTopProspectsDetailed` — `/prospects`/`/TBL/prospects`) used to fetch the `players` table FIRST — unfiltered, up to the full ~45,757-row table when no `orgId`/`playerIds` scope applied, which is exactly the leaguewide default view both prospects pages use — then chunk-fetched `player_computed` and `player_ratings_snapshots` for **all** of them in batches of 500, and only THEN sorted and sliced down to the actual 500 needed, in JS. That's 230+ sequential round trips to build a 500-row list. Fixed by querying `player_computed` FIRST, with the sort (`overall`/`prospect_potential` descending) and limit (`+50` buffer for safety) applied IN the query — then only fetching `players`/ratings for that much smaller resulting set. The org-scoped case (one team) was already fine and is unchanged in shape, just now also limited at the DB level for consistency.
2. **`getOrgTeams()`** (powers the team filter dropdown on every list page, and `getTeamRankings()` for System Rankings) used to fetch every player's `organization_id` — again up to the full ~45,757-row table, unfiltered — just to build a Set of which of the ~30-40 candidate "parent" teams actually have players attached (filtering out placeholder/conference rows like "ODC Fire Conference"). Fixed by getting the small candidate team list first, then checking each candidate for at least one matching player **in parallel** (`Promise.all`, not sequential) — wall-clock cost is now roughly one round trip, not ~46 sequential ones.
3. **`getProspectSnapshotOptions()`** (powers the "Change from" baseline picker) — this was the dominant remaining cost once the two above were fixed, confirmed by temporarily instrumenting every step with `console.log` timers (removed before committing) and finding the actual query work totaled ~1.4s while the reported request time was still ~6.9s. It used to fetch `refresh_run_id` from **every row** of `player_computed` — one row per player *per historical refresh run*, so potentially hundreds of thousands of rows — just to dedupe down to a handful of distinct ids, exactly the comment already flagged as a future risk ("worth revisiting if this ever gets slow"). Fixed the same way as `getOrgTeams`: query the small `refresh_runs` table first (one row per actual refresh event, capped at the 30 most recent — plenty for a "change from" picker), then confirm each candidate has a real `player_computed` snapshot via small parallel existence checks.

**Net result, measured directly in dev server logs (warm request, no compile overhead): ~20-23 seconds → ~1.6-1.9 seconds.** Verified correctness held throughout, not just speed: `/TBL/prospects`'s rankings (#1 R.J. Blum, #2 J.P. Schall, ...), row count (1000 = 500 players × 2 rows), the "Change from" dropdown's two dates, `/TBL/prospects/farms`'s team order (#1 Fort Collins Roosters, ...), and the org filter's full 32-team list all matched exactly what they showed before any of these changes. **If a future page feels slow, check for this same pattern first** — an unfiltered `fetchAll()` over `players` or `player_computed` with no `.limit()` pushed into the query is the one to look for; both are large, frequently-queried tables where "fetch everything, filter in JS" silently turns into hundreds of sequential round trips as the league's history grows.

**Step 6 of the visual refresh: motion, and a real environment limitation discovered along the way, 2026-08-25.** Two small CSS-only additions, both in `app/globals.css`: a brass accent stripe (`border-left`, reserved at zero-opacity so it never shifts layout, only recolors) on the first cell of a hovered row, sitewide; and a small pulsing dot before the "Data as of" game-date badge (`.site-game-date::before`, `@keyframes dsa-pulse`), signaling "this is live data" without making the date text itself distracting to read. Both respect `prefers-reduced-motion`.

The third piece — fading `ProspectTable`'s H/P/Role filter results in instead of hard-snapping — took a real detour worth recording. First attempt used the **Motion** library (`motion.div`, `initial={{opacity:0}} animate={{opacity:1}}`), which appeared to fail: the element read back as permanently stuck at `opacity: 0` in computed style, on both the first SSR-hydrated mount and a purely client-triggered remount. Swapping to a plain CSS `@keyframes` animation as a "simpler, can't-get-stuck" fallback showed the *exact same symptom* — which is what actually cracked the real cause: `document.timeline.currentTime` reads a frozen `0` in this session's Browser pane no matter how long a page sits idle. **This session's Browser pane isn't advancing its animation/compositor clock at all** — consistent with screenshot capture failing all session with "not compositing frames" — so neither Motion nor CSS animations could ever be observed actually progressing here, independent of whether either was written correctly. The `motion` package was installed, then removed again once this became clear, since the CSS version needs no extra dependency and there was no remaining reason to prefer the more complex option. **Bottom line: the fade-in's visual correctness was never actually verified this session** — only that it's wired up structurally (`animation-name`/`animation-duration` present in computed style, the underlying filter logic still works, no console errors). Confirm live that it actually looks right, same as the earlier logo swaps. If a future session hits a similar "why isn't this animating" mystery, check `document.timeline.currentTime` early — it'll immediately tell you whether the problem is the code or the environment.

**Step 5 of the visual refresh: table/card polish, 2026-08-25.** Checked the site's actual table CSS against the approved plan's before/after mockup before touching anything — most of it (`.table-wrap`'s shadow, row hover, `tabular-nums`) was already in place from earlier work, not new for this step. The one real gap: `th` still had the *original* flat-tan header band, not the dark-navy-with-brass-underline treatment the plan's mockup actually showed. Fixed in `app/globals.css`'s single shared `th` rule — applies to every table sitewide (Top Prospects, System Rankings, Top Players, Top Draftees) in one place, plus a `tbody tr { transition: background-color 0.12s ease }` so the hover highlight fades instead of snapping (free, CSS-only precursor to Step 6's real motion work). Separately, `app/org-minors/MinorsTable.tsx` — named in the original plan, but its own bespoke inline-styled tables (not the shared `table`/`th` CSS, so the change above doesn't reach it) had drifted furthest from the rest of the site: a hardcoded `system-ui` font ignoring the site's actual Fraunces/Inter choice, raw hex colors (`#3366cc`, `#555`, `#333`, `#444`) instead of the design tokens, and no card shadow at all. Brought in line: `var(--font-body)`, the same navy/brass header treatment as everywhere else, `var(--color-border-strong)`/`var(--color-border)` in place of the hardcoded grays, and `var(--shadow-sm)`/`var(--radius-md)` on the per-team cards. The gradient legend colors (red→orange→yellow→green→blue for Overall/Potential grades) were deliberately left alone — that's a data-visualization scale, not site chrome, and was never part of this palette. Verified live (owner cookie set directly, not via the login form): `th` background/text/border-bottom sampled on `/TBL/prospects` match the new values exactly; `/org-minors` card shadow, radius, and body font all confirmed matching too; console clean.

**Step 4 of the visual refresh: palette + typography rolled out sitewide, 2026-08-25.** `app/globals.css`'s `:root` block — the `--color-*` tokens, the *only* ones any existing component actually renders with (128 usages across 7 files, confirmed by grep before touching anything) — updated from the original navy-and-tan sampled off the brand logo to the deepened/warmed palette from the approved "DSA Visual Refresh" plan (navy `#002030`→`#0B3049`, tan `#c09050`→ brass `#A8763A`, etc.), plus every derived/auxiliary token (hover states, muted text, table stripes) shifted to match. `app/layout.tsx`'s fonts swapped from Libre Franklin (both roles) to **Fraunces** (display/headings) + **Inter** (body) — the pairing from the same plan — via the same `--font-display`/`--font-body` CSS variable names, so no component needed to change. This is the first change that actually applies the shadcn tokens' *sibling* system (the real, separate `--color-*` block) — the `--background`/`--primary`/etc. tokens populated in Step 2 are still a different, still-mostly-inert system for future shadcn components, unaffected by and unrelated to this change. Verified live: background/heading-color/header-bg/border/table-header-bg all sampled via computed styles and matched the new hex values exactly; both fonts confirmed loading (`__Fraunces_*`, `__Inter_*` in `fontFamily`); console clean.

**Public pages split and moved under `/TBL/prospects`, 2026-08-25.** The combined report that used to live at `/report` (Top Prospects + System Rankings on one page) is now two separate pages: `/TBL/prospects` (Top Prospects only, `FarmSystemReportBody`'s new `showRankings={false}` mode) and `/TBL/prospects/farms` (System Rankings only, a small standalone page reusing `TeamRankingsTable` directly). Both are linked from `ReportHeader`'s new two-item nav (previously just a logo + login link, back when there was only one public page to point at). `/report` itself is now a permanent `redirect("/TBL/prospects")` page, not deleted outright, so any already-shared/bookmarked link keeps working for guest and owner alike. The `TBL` prefix (TheBigLeague, the league this whole site covers) is Rees's own URL scheme, deliberately leaving room to host other leagues under `dsa-reports.com` later without a path collision — nothing else on the site uses that prefix yet (`/players`, `/draft`, etc. are untouched). `/prospects` (the internal, owner-only combined page) is unaffected — `showRankings` defaults to `true`, so it keeps the original side-by-side layout without passing anything.

**New Vercel project: `tbl-dsa`** (not a reuse of the older `dsa-report` project, which is a separate, still-live, currently-unprotected deployment of stale pre-session code at `dsa-report-lilac.vercel.app` — Rees explicitly chose "set up somewhere new" rather than redeploy over it; that old project's fate hasn't been decided yet). Deployed via the `deploy_to_vercel` MCP tool (no git repo involved for this deploy — Vercel CLI wasn't authenticated locally and there's no env-var-setting MCP tool, so this was the only viable path). **Critical mechanic learned the hard way**: every `deploy_to_vercel` call is a full, standalone, complete-*replacement* deployment, never additive across calls — omitting a file that was included in an earlier call removes it, it doesn't merge. Took ~10 attempts to land a build with the complete file set; `get_deployment_build_logs` (not manual bookkeeping) is the reliable way to find out exactly which files/errors remain.

Domain: Rees is mid-registration on a custom domain via Wix — **explicitly paused, do not connect a custom domain until he gives the go-ahead** — tonight's deploy uses the Vercel-assigned URL only.

`public/logo.png` was omitted from tonight's deploy payload (523KB → ~700K base64 characters, too costly for one tool call) — expect a broken image icon in the nav on owner-facing pages until a lighter-weight path (e.g. committing the repo to git and deploying from there instead of inline files) is worked out.
